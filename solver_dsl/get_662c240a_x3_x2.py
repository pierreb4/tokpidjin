def get_662c240a_x3_x2() -> Callable:
    return fork(equality, rbind(mir_rot_t, R1), identity)

# {'return': 'Callable'}

func_d = {('get_662c240a_x3_x2', 'Callable'): 'fork(equality, rbind(mir_rot_t, R1), identity)'}

