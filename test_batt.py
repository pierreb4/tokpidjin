# Generated by tokpidjin/card.py

from pile import *
from gpu_env import GPUEnv as Env
from safe_dsl import _get_safe_default
from batt_gpu import batch_process_samples_gpu

def batt(task_id, S, I, C, log_path):
    s = []
    o = []
    # Pre-mutate: t1 - differ = True - solver = False - old_items = ['identity', ' p_g'] - old_hints = ['Any', 'Any']
    try:
        t1 = identity(p_g)
    except (TypeError, AttributeError, ValueError):
        t1 = _get_safe_default(identity)
    # Pre-mutate: t2 - differ = True - solver = False - old_items = ['t1', ' I'] - old_hints = None
    try:
        t2 = t1(t1)
    except (TypeError, AttributeError, ValueError):
        t2 = _get_safe_default(t1)
    # Pre-mutate: t3 - differ = True - solver = False - old_items = ['t1', ' C'] - old_hints = None
    try:
        t3 = t1(C)
    except (TypeError, AttributeError, ValueError):
        t3 = _get_safe_default(t1)
    # Pre-mutate: t4 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t2'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t4 = difference_tuple(t3, t2)
    except (TypeError, AttributeError, ValueError):
        t4 = _get_safe_default(difference_tuple)
    # Pre-mutate: t5 - differ = True - solver = False - old_items = ['size', ' t4'] - old_hints = ['Integer', 'Container']
    try:
        t5 = size(t4)
    except (TypeError, AttributeError, ValueError):
        t5 = _get_safe_default(size)
    # Pre-mutate: t6 - differ = True - solver = True - old_items = ['get_nth_t', ' t4', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t6 = get_nth_t(t4, F0)
    except (TypeError, AttributeError, ValueError):
        t6 = _get_safe_default(get_nth_t)
    # Pre-mutate: t7 - differ = True - solver = False - old_items = ['astuple', ' t5', ' t6'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t7 = astuple(t5, t6)
    except (TypeError, AttributeError, ValueError):
        t7 = _get_safe_default(astuple)
    s.append((7, 'None', 'differ_c59e6ae069a9205b311cf67a15473d64', t7))
    # Pre-mutate: t8 - differ = True - solver = False - old_items = ['rbind', ' o_g', ' R5'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t8 = rbind(o_g, R5)
    except (TypeError, AttributeError, ValueError):
        t8 = _get_safe_default(rbind)
    # Pre-mutate: t9 - differ = True - solver = False - old_items = ['t8', ' I'] - old_hints = None
    try:
        t9 = t8(I)
    except (TypeError, AttributeError, ValueError):
        t9 = _get_safe_default(t8)
    # Pre-mutate: t10 - differ = True - solver = False - old_items = ['t8', ' C'] - old_hints = None
    try:
        t10 = t8(C)
    except (TypeError, AttributeError, ValueError):
        t10 = _get_safe_default(t8)
    # Pre-mutate: t11 - differ = True - solver = False - old_items = ['rbind', ' sizefilter', ' ONE'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t11 = rbind(sizefilter, ONE)
    except (TypeError, AttributeError, ValueError):
        t11 = _get_safe_default(rbind)
    # Pre-mutate: t12 - differ = True - solver = False - old_items = ['t11', ' t9'] - old_hints = None
    try:
        t12 = t11(t9)
    except (TypeError, AttributeError, ValueError):
        t12 = _get_safe_default(t11)
    # Pre-mutate: t13 - differ = True - solver = False - old_items = ['t11', ' t10'] - old_hints = None
    try:
        t13 = t11(t10)
    except (TypeError, AttributeError, ValueError):
        t13 = _get_safe_default(t11)
    # Pre-mutate: t14 - differ = True - solver = False - old_items = ['difference', ' t12', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t14 = difference(t12, t13)
    except (TypeError, AttributeError, ValueError):
        t14 = _get_safe_default(difference)
    # Pre-mutate: t15 - differ = True - solver = False - old_items = ['size', ' t14'] - old_hints = ['Integer', 'Container']
    try:
        t15 = size(t14)
    except (TypeError, AttributeError, ValueError):
        t15 = _get_safe_default(size)
    # Pre-mutate: t16 - differ = True - solver = True - old_items = ['get_nth_f', ' t14', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t16 = get_nth_f(t14, F0)
    except (TypeError, AttributeError, ValueError):
        t16 = _get_safe_default(get_nth_f)
    # Pre-mutate: t17 - differ = True - solver = False - old_items = ['astuple', ' t15', ' t16'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t17 = astuple(t15, t16)
    except (TypeError, AttributeError, ValueError):
        t17 = _get_safe_default(astuple)
    s.append((17, 'None', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t17))
    # Pre-mutate: t18 - differ = True - solver = False - old_items = ['difference', ' t13', ' t12'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t18 = difference(t13, t12)
    except (TypeError, AttributeError, ValueError):
        t18 = _get_safe_default(difference)
    # Pre-mutate: t19 - differ = True - solver = False - old_items = ['size', ' t18'] - old_hints = ['Integer', 'Container']
    try:
        t19 = size(t18)
    except (TypeError, AttributeError, ValueError):
        t19 = _get_safe_default(size)
    # Pre-mutate: t20 - differ = True - solver = True - old_items = ['get_nth_f', ' t18', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t20 = get_nth_f(t18, F0)
    except (TypeError, AttributeError, ValueError):
        t20 = _get_safe_default(get_nth_f)
    # Pre-mutate: t21 - differ = True - solver = False - old_items = ['astuple', ' t19', ' t20'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t21 = astuple(t19, t20)
    except (TypeError, AttributeError, ValueError):
        t21 = _get_safe_default(astuple)
    s.append((21, 'None', 'differ_f4a4346405e974b919a4b5a52f28e40c', t21))
    # Pre-mutate: t22 - differ = True - solver = False - old_items = ['difference_tuple', ' t2', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t22 = difference_tuple(t2, t3)
    except (TypeError, AttributeError, ValueError):
        t22 = _get_safe_default(difference_tuple)
    # Pre-mutate: t23 - differ = True - solver = False - old_items = ['size', ' t22'] - old_hints = ['Integer', 'Container']
    try:
        t23 = size(t22)
    except (TypeError, AttributeError, ValueError):
        t23 = _get_safe_default(size)
    # Pre-mutate: t24 - differ = True - solver = True - old_items = ['get_nth_t', ' t22', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t24 = get_nth_t(t22, F0)
    except (TypeError, AttributeError, ValueError):
        t24 = _get_safe_default(get_nth_t)
    # Pre-mutate: t25 - differ = True - solver = False - old_items = ['astuple', ' t23', ' t24'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t25 = astuple(t23, t24)
    except (TypeError, AttributeError, ValueError):
        t25 = _get_safe_default(astuple)
    s.append((25, 'None', 'differ_fc9e44c913711da609a9e25126b4b296', t25))
    # Pre-mutate: t26 - differ = False - solver = True - old_items = ['get_color_rank_t', ' I', ' L1'] - old_hints = ['C_', 'Grid', 'FL']
    try:
        t26 = get_color_rank_t(I, L1)
    except (TypeError, AttributeError, ValueError):
        t26 = _get_safe_default(get_color_rank_t)
    # Pre-mutate: t27 - differ = False - solver = True - old_items = ['fgpartition', ' I'] - old_hints = ['Objects', 'Grid']
    try:
        t27 = fgpartition(I)
    except (TypeError, AttributeError, ValueError):
        t27 = _get_safe_default(fgpartition)
    # Pre-mutate: t28 - differ = False - solver = True - old_items = ['o_g_t', ' I', ' R4'] - old_hints = ['Tuple[Tuple[Tuple[int, int, int], ...], ...]', 'Grid', 'R8']
    try:
        t28 = o_g_t(I, R4)
    except (TypeError, AttributeError, ValueError):
        t28 = _get_safe_default(o_g_t)
    # Pre-mutate: t29 - differ = False - solver = True - old_items = ['rbind', ' get_nth_f', ' F0'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t29 = rbind(get_nth_f, F0)
    except (TypeError, AttributeError, ValueError):
        t29 = _get_safe_default(rbind)
    # Pre-mutate: t30 - differ = False - solver = True - old_items = ['o_g_t', ' I', ' R5'] - old_hints = ['Tuple[Tuple[Tuple[int, int, int], ...], ...]', 'Grid', 'R8']
    try:
        t30 = o_g_t(I, R5)
    except (TypeError, AttributeError, ValueError):
        t30 = _get_safe_default(o_g_t)
    # Pre-mutate: t31 - differ = False - solver = True - old_items = ['f_ofcolor', ' I', ' THREE'] - old_hints = ['Indices', 'Grid', 'C_']
    try:
        t31 = f_ofcolor(I, GREEN)
    except (TypeError, AttributeError, ValueError):
        t31 = _get_safe_default(f_ofcolor)
    # Pre-mutate: t32 - differ = False - solver = True - old_items = ['fork', ' combine', ' vfrontier', ' hfrontier'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t32 = fork(combine, vfrontier, hfrontier)
    except (TypeError, AttributeError, ValueError):
        t32 = _get_safe_default(fork)
    # Pre-mutate: t33 - differ = False - solver = True - old_items = ['replace', ' I', ' EIGHT', ' ZERO'] - old_hints = ['Grid', 'Grid', 'C_', 'C_']
    # GPU Batch Pattern: Sample processing (t33-t36)
    t33, t34, t35, t36 = batch_process_samples_gpu(S)
    t37 = dedupe(t35)
    t38 = dedupe(t36)
    t39 = difference_tuple(t37, t38)
    t40 = get_nth_t(t39, F0)
    try:
        t41 = replace(I, EIGHT, BLACK)
    except (TypeError, AttributeError, ValueError):
        t41 = _get_safe_default(replace)
    # Pre-mutate: t42 - differ = False - solver = True - old_items = ['o_g_t', ' I', ' R3'] - old_hints = ['Tuple[Tuple[Tuple[int, int, int], ...], ...]', 'Grid', 'R8']
    try:
        t42 = o_g_t(I, R3)
    except (TypeError, AttributeError, ValueError):
        t42 = _get_safe_default(o_g_t)
    # Pre-mutate: t43 - differ = False - solver = True - old_items = ['rbind', ' upscale_f', ' TWO'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t43 = rbind(upscale_f, TWO)
    except (TypeError, AttributeError, ValueError):
        t43 = _get_safe_default(rbind)
    # Pre-mutate: t44 - differ = False - solver = True - old_items = ['height_t', ' I'] - old_hints = ['Integer', 'Tuple']
    try:
        t44 = height_t(I)
    except (TypeError, AttributeError, ValueError):
        t44 = _get_safe_default(height_t)
    # Pre-mutate: t45 - differ = False - solver = True - old_items = ['f_ofcolor', ' I', ' TWO'] - old_hints = ['Indices', 'Grid', 'C_']
    try:
        t45 = f_ofcolor(I, RED)
    except (TypeError, AttributeError, ValueError):
        t45 = _get_safe_default(f_ofcolor)
    # Pre-mutate: t46 - differ = False - solver = True - old_items = ['o_g_t', ' I', ' R7'] - old_hints = ['Tuple[Tuple[Tuple[int, int, int], ...], ...]', 'Grid', 'R8']
    try:
        t46 = o_g_t(I, R7)
    except (TypeError, AttributeError, ValueError):
        t46 = _get_safe_default(o_g_t)
    # Pre-mutate: t47 - differ = False - solver = True - old_items = ['canvas', ' ZERO', ' THREE_BY_THREE'] - old_hints = ['Grid', 'C_', 'IJ']
    try:
        t47 = canvas(BLACK, THREE_BY_THREE)
    except (TypeError, AttributeError, ValueError):
        t47 = _get_safe_default(canvas)
    # Pre-mutate: t48 - differ = False - solver = True - old_items = ['mir_rot_t', ' I', ' R2'] - old_hints = ['Grid', 'Grid', 'A8']
    try:
        t48 = mir_rot_t(I, R2)
    except (TypeError, AttributeError, ValueError):
        t48 = _get_safe_default(mir_rot_t)
    # Pre-mutate: t49 - differ = False - solver = True - old_items = ['astuple', ' SIX', ' SIX'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t49 = astuple(SIX, SIX)
    except (TypeError, AttributeError, ValueError):
        t49 = _get_safe_default(astuple)
    # Pre-mutate: t50 - differ = False - solver = True - old_items = ['f_ofcolor', ' I', ' t26'] - old_hints = ['Indices', 'Grid', 'C_']
    try:
        t50 = f_ofcolor(I, t26)
    except (TypeError, AttributeError, ValueError):
        t50 = _get_safe_default(f_ofcolor)
    # Pre-mutate: t51 - differ = False - solver = True - old_items = ['merge_t', ' t27'] - old_hints = ['Container', 'ContainerContainer']
    try:
        t51 = merge_t(t27)
    except (TypeError, AttributeError, ValueError):
        t51 = _get_safe_default(merge_t)
    # Pre-mutate: t52 - differ = False - solver = True - old_items = ['colorfilter_t', ' t28', ' ZERO'] - old_hints = ['Tuple[Tuple[Tuple[int, int, int], ...], ...]', 'Tuple[Tuple[Tuple[int, int, int], ...], ...]', 'Integer']
    try:
        t52 = colorfilter_t(t28, ZERO)
    except (TypeError, AttributeError, ValueError):
        t52 = _get_safe_default(colorfilter_t)
    # Pre-mutate: t53 - differ = False - solver = True - old_items = ['compose', ' center', ' t29'] - old_hints = ['Callable', 'Callable', 'Callable']
    try:
        t53 = compose(center, t29)
    except (TypeError, AttributeError, ValueError):
        t53 = _get_safe_default(compose)
    # Pre-mutate: t54 - differ = False - solver = True - old_items = ['merge', ' t30'] - old_hints = ['Container', 'ContainerContainer']
    try:
        t54 = merge(t30)
    except (TypeError, AttributeError, ValueError):
        t54 = _get_safe_default(merge)
    # Pre-mutate: t55 - differ = False - solver = True - old_items = ['mapply_t', ' neighbors', ' t31'] - old_hints = ['Tuple', 'Callable', 'Tuple']
    try:
        t55 = mapply_t(neighbors, t31)
    except (TypeError, AttributeError, ValueError):
        t55 = _get_safe_default(mapply_t)
    # Pre-mutate: t56 - differ = False - solver = True - old_items = ['compose', ' t32', ' center'] - old_hints = ['Callable', 'Callable', 'Callable']
    try:
        t56 = compose(t32, center)
    except (TypeError, AttributeError, ValueError):
        t56 = _get_safe_default(compose)
    # Pre-mutate: t57 - differ = False - solver = True - old_items = ['get_color_rank_t', ' t41', ' L1'] - old_hints = ['C_', 'Grid', 'FL']
    try:
        t57 = get_color_rank_t(t41, L1)
    except (TypeError, AttributeError, ValueError):
        t57 = _get_safe_default(get_color_rank_t)
    # Pre-mutate: t58 - differ = False - solver = True - old_items = ['get_arg_rank_t', ' t42', ' numcolors_f', ' L1'] - old_hints = ['Any', 'Tuple', 'Callable', 'FL']
    try:
        t58 = get_arg_rank_t(t42, numcolors_f, L1)
    except (TypeError, AttributeError, ValueError):
        t58 = _get_safe_default(get_arg_rank_t)
    # Pre-mutate: t59 - differ = False - solver = True - old_items = ['rbind', ' mir_rot_f', ' R2'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t59 = rbind(mir_rot_f, R2)
    except (TypeError, AttributeError, ValueError):
        t59 = _get_safe_default(rbind)
    # Pre-mutate: t60 - differ = False - solver = True - old_items = ['matcher', ' size', ' SIX'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t60 = matcher(size, SIX)
    except (TypeError, AttributeError, ValueError):
        t60 = _get_safe_default(matcher)
    # Pre-mutate: t61 - differ = False - solver = True - old_items = ['compose', ' toindices', ' normalize'] - old_hints = ['Callable', 'Callable', 'Callable']
    try:
        t61 = compose(toindices, normalize)
    except (TypeError, AttributeError, ValueError):
        t61 = _get_safe_default(compose)
    # Pre-mutate: t62 - differ = False - solver = True - old_items = ['decrement', ' t44'] - old_hints = ['Numerical', 'Numerical']
    try:
        t62 = decrement(t44)
    except (TypeError, AttributeError, ValueError):
        t62 = _get_safe_default(decrement)
    # Pre-mutate: t63 - differ = False - solver = True - old_items = ['recolor_i', ' TWO', ' t45'] - old_hints = ['Object', 'C_', 'Indices']
    try:
        t63 = recolor_i(RED, t45)
    except (TypeError, AttributeError, ValueError):
        t63 = _get_safe_default(recolor_i)
    # Pre-mutate: t64 - differ = False - solver = True - old_items = ['mapply_t', ' delta', ' t46'] - old_hints = ['Tuple', 'Callable', 'Tuple']
    try:
        t64 = mapply_t(delta, t46)
    except (TypeError, AttributeError, ValueError):
        t64 = _get_safe_default(mapply_t)
    # Pre-mutate: t65 - differ = False - solver = True - old_items = ['f_ofcolor', ' I', ' t46'] - old_hints = ['Indices', 'Grid', 'C_']
    try:
        t65 = f_ofcolor(I, t46)
    except (TypeError, AttributeError, ValueError):
        t65 = _get_safe_default(f_ofcolor)
    # Pre-mutate: t66 - differ = False - solver = True - old_items = ['hconcat', ' I', ' t48'] - old_hints = ['Grid', 'Grid', 'Grid']
    try:
        t66 = hconcat(I, t48)
    except (TypeError, AttributeError, ValueError):
        t66 = _get_safe_default(hconcat)
    # Pre-mutate: t67 - differ = False - solver = True - old_items = ['canvas', ' ZERO', ' t49'] - old_hints = ['Grid', 'C_', 'IJ']
    try:
        t67 = canvas(BLACK, t49)
    except (TypeError, AttributeError, ValueError):
        t67 = _get_safe_default(canvas)
    # Pre-mutate: t68 - differ = False - solver = True - old_items = ['center', ' t50'] - old_hints = ['IJ', 'Patch']
    try:
        t68 = center(t50)
    except (TypeError, AttributeError, ValueError):
        t68 = _get_safe_default(center)
    # Pre-mutate: t69 - differ = False - solver = True - old_items = ['cover', ' I', ' t51'] - old_hints = ['Grid', 'Grid', 'Patch']
    try:
        t69 = cover(I, t51)
    except (TypeError, AttributeError, ValueError):
        t69 = _get_safe_default(cover)
    # Pre-mutate: t70 - differ = False - solver = True - old_items = ['get_arg_rank_t', ' t51', ' size', ' F0'] - old_hints = ['Any', 'Tuple', 'Callable', 'FL']
    try:
        t70 = get_arg_rank_t(t51, size, F0)
    except (TypeError, AttributeError, ValueError):
        t70 = _get_safe_default(get_arg_rank_t)
    # Pre-mutate: t71 - differ = False - solver = True - old_items = ['fork', ' equality', ' toindices', ' backdrop'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t71 = fork(equality, toindices, backdrop)
    except (TypeError, AttributeError, ValueError):
        t71 = _get_safe_default(fork)
    # Pre-mutate: t72 - differ = False - solver = True - old_items = ['rbind', ' get_nth_f', ' L1'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t72 = rbind(get_nth_f, L1)
    except (TypeError, AttributeError, ValueError):
        t72 = _get_safe_default(rbind)
    # Pre-mutate: t73 - differ = False - solver = True - old_items = ['lbind', ' shift', ' t54'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t73 = lbind(shift, t54)
    except (TypeError, AttributeError, ValueError):
        t73 = _get_safe_default(lbind)
    # Pre-mutate: t74 - differ = False - solver = True - old_items = ['fill', ' I', ' SIX', ' t55'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t74 = fill(I, MAGENTA, t55)
    except (TypeError, AttributeError, ValueError):
        t74 = _get_safe_default(fill)
    # Pre-mutate: t75 - differ = False - solver = True - old_items = ['other_f', ' t42', ' t58'] - old_hints = ['Any', 'FrozenSet', 'Any']
    try:
        t75 = other_f(t42, t58)
    except (TypeError, AttributeError, ValueError):
        t75 = _get_safe_default(other_f)
    # Pre-mutate: t76 - differ = False - solver = True - old_items = ['compose', ' t43', ' t59'] - old_hints = ['Callable', 'Callable', 'Callable']
    try:
        t76 = compose(t43, t59)
    except (TypeError, AttributeError, ValueError):
        t76 = _get_safe_default(compose)
    # Pre-mutate: t77 - differ = False - solver = True - old_items = ['mfilter_f', ' t43', ' t60'] - old_hints = ['FrozenSet', 'FrozenSet', 'Callable']
    try:
        t77 = mfilter_f(t43, t60)
    except (TypeError, AttributeError, ValueError):
        t77 = _get_safe_default(mfilter_f)
    # Pre-mutate: t78 - differ = False - solver = True - old_items = ['crop', ' I', ' ORIGIN', ' THREE_BY_THREE'] - old_hints = ['Grid', 'Grid', 'IJ', 'IJ']
    try:
        t78 = crop(I, ORIGIN, THREE_BY_THREE)
    except (TypeError, AttributeError, ValueError):
        t78 = _get_safe_default(crop)
    # Pre-mutate: t79 - differ = False - solver = True - old_items = ['decrement', ' t62'] - old_hints = ['Numerical', 'Numerical']
    try:
        t79 = decrement(t62)
    except (TypeError, AttributeError, ValueError):
        t79 = _get_safe_default(decrement)
    # Pre-mutate: t80 - differ = False - solver = True - old_items = ['fill', ' I', ' SEVEN', ' t64'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    # GPU Batch Pattern: Sample processing (t80-t83)
    t80, t81, t82, t83 = batch_process_samples_gpu(S)
    t84 = dedupe(t82)
    t85 = dedupe(t83)
    t86 = difference_tuple(t85, t84)
    t87 = get_nth_t(t86, F0)
    try:
        t88 = fill(I, t87, t64)
    except (TypeError, AttributeError, ValueError):
        t88 = _get_safe_default(fill)
    o.append((88, True, '60b61512', t88))
    # Pre-mutate: t89 - differ = True - solver = False - old_items = ['t1', ' t88'] - old_hints = None
    try:
        t89 = t1(t88)
    except (TypeError, AttributeError, ValueError):
        t89 = _get_safe_default(t1)
    # Pre-mutate: t90 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t89'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t90 = difference_tuple(t3, t89)
    except (TypeError, AttributeError, ValueError):
        t90 = _get_safe_default(difference_tuple)
    # Pre-mutate: t91 - differ = True - solver = False - old_items = ['size', ' t90'] - old_hints = ['Integer', 'Container']
    try:
        t91 = size(t90)
    except (TypeError, AttributeError, ValueError):
        t91 = _get_safe_default(size)
    # Pre-mutate: t92 - differ = True - solver = True - old_items = ['get_nth_t', ' t90', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t92 = get_nth_t(t90, F0)
    except (TypeError, AttributeError, ValueError):
        t92 = _get_safe_default(get_nth_t)
    # Pre-mutate: t93 - differ = True - solver = False - old_items = ['astuple', ' t91', ' t92'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t93 = astuple(t91, t92)
    except (TypeError, AttributeError, ValueError):
        t93 = _get_safe_default(astuple)
    s.append((93, '60b61512', 'differ_c59e6ae069a9205b311cf67a15473d64', t93))
    # Pre-mutate: t94 - differ = True - solver = False - old_items = ['t8', ' t88'] - old_hints = None
    try:
        t94 = t8(t88)
    except (TypeError, AttributeError, ValueError):
        t94 = _get_safe_default(t8)
    # Pre-mutate: t95 - differ = True - solver = False - old_items = ['t11', ' t94'] - old_hints = None
    try:
        t95 = t11(t94)
    except (TypeError, AttributeError, ValueError):
        t95 = _get_safe_default(t11)
    # Pre-mutate: t96 - differ = True - solver = False - old_items = ['difference', ' t95', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t96 = difference(t95, t13)
    except (TypeError, AttributeError, ValueError):
        t96 = _get_safe_default(difference)
    # Pre-mutate: t97 - differ = True - solver = False - old_items = ['size', ' t96'] - old_hints = ['Integer', 'Container']
    try:
        t97 = size(t96)
    except (TypeError, AttributeError, ValueError):
        t97 = _get_safe_default(size)
    # Pre-mutate: t98 - differ = True - solver = True - old_items = ['get_nth_f', ' t96', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t98 = get_nth_f(t96, F0)
    except (TypeError, AttributeError, ValueError):
        t98 = _get_safe_default(get_nth_f)
    # Pre-mutate: t99 - differ = True - solver = False - old_items = ['astuple', ' t97', ' t98'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t99 = astuple(t97, t98)
    except (TypeError, AttributeError, ValueError):
        t99 = _get_safe_default(astuple)
    s.append((99, '60b61512', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t99))
    # Pre-mutate: t100 - differ = True - solver = False - old_items = ['difference', ' t13', ' t95'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t100 = difference(t13, t95)
    except (TypeError, AttributeError, ValueError):
        t100 = _get_safe_default(difference)
    # Pre-mutate: t101 - differ = True - solver = False - old_items = ['size', ' t100'] - old_hints = ['Integer', 'Container']
    try:
        t101 = size(t100)
    except (TypeError, AttributeError, ValueError):
        t101 = _get_safe_default(size)
    # Pre-mutate: t102 - differ = True - solver = True - old_items = ['get_nth_f', ' t100', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t102 = get_nth_f(t100, F0)
    except (TypeError, AttributeError, ValueError):
        t102 = _get_safe_default(get_nth_f)
    # Pre-mutate: t103 - differ = True - solver = False - old_items = ['astuple', ' t101', ' t102'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t103 = astuple(t101, t102)
    except (TypeError, AttributeError, ValueError):
        t103 = _get_safe_default(astuple)
    s.append((103, '60b61512', 'differ_f4a4346405e974b919a4b5a52f28e40c', t103))
    # Pre-mutate: t104 - differ = True - solver = False - old_items = ['difference_tuple', ' t89', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t104 = difference_tuple(t89, t3)
    except (TypeError, AttributeError, ValueError):
        t104 = _get_safe_default(difference_tuple)
    # Pre-mutate: t105 - differ = True - solver = False - old_items = ['size', ' t104'] - old_hints = ['Integer', 'Container']
    try:
        t105 = size(t104)
    except (TypeError, AttributeError, ValueError):
        t105 = _get_safe_default(size)
    # Pre-mutate: t106 - differ = True - solver = True - old_items = ['get_nth_t', ' t104', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t106 = get_nth_t(t104, F0)
    except (TypeError, AttributeError, ValueError):
        t106 = _get_safe_default(get_nth_t)
    # Pre-mutate: t107 - differ = True - solver = False - old_items = ['astuple', ' t105', ' t106'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t107 = astuple(t105, t106)
    except (TypeError, AttributeError, ValueError):
        t107 = _get_safe_default(astuple)
    s.append((107, '60b61512', 'differ_fc9e44c913711da609a9e25126b4b296', t107))
    # Pre-mutate: t108 - differ = False - solver = True - old_items = ['mapply_t', ' vfrontier', ' t65'] - old_hints = ['Tuple', 'Callable', 'Tuple']
    try:
        t108 = mapply_t(vfrontier, t65)
    except (TypeError, AttributeError, ValueError):
        t108 = _get_safe_default(mapply_t)
    # Pre-mutate: t109 - differ = False - solver = True - old_items = ['rbind', ' f_ofcolor', ' t65'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t109 = rbind(f_ofcolor, t65)
    except (TypeError, AttributeError, ValueError):
        t109 = _get_safe_default(rbind)
    # Pre-mutate: t110 - differ = False - solver = True - old_items = ['mir_rot_t', ' t66', ' R0'] - old_hints = ['Grid', 'Grid', 'A8']
    try:
        t110 = mir_rot_t(t66, R0)
    except (TypeError, AttributeError, ValueError):
        t110 = _get_safe_default(mir_rot_t)
    # Pre-mutate: t111 - differ = False - solver = True - old_items = ['rbind', ' shoot', ' UNITY'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t111 = rbind(shoot, UNITY)
    except (TypeError, AttributeError, ValueError):
        t111 = _get_safe_default(rbind)
    # Pre-mutate: t112 - differ = False - solver = True - old_items = ['corner', ' t50', ' R0'] - old_hints = ['IJ', 'Patch', 'R4']
    try:
        t112 = corner(t50, R0)
    except (TypeError, AttributeError, ValueError):
        t112 = _get_safe_default(corner)
    # Pre-mutate: t113 - differ = False - solver = True - old_items = ['toindices', ' t70'] - old_hints = ['Indices', 'Patch']
    try:
        t113 = toindices(t70)
    except (TypeError, AttributeError, ValueError):
        t113 = _get_safe_default(toindices)
    # Pre-mutate: t114 - differ = False - solver = True - old_items = ['sfilter_f', ' t52', ' t71'] - old_hints = ['FrozenSet', 'FrozenSet', 'Callable']
    try:
        t114 = sfilter_f(t52, t71)
    except (TypeError, AttributeError, ValueError):
        t114 = _get_safe_default(sfilter_f)
    # Pre-mutate: t115 - differ = False - solver = True - old_items = ['fork', ' gravitate', ' t29', ' t72'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t115 = fork(gravitate, t29, t72)
    except (TypeError, AttributeError, ValueError):
        t115 = _get_safe_default(fork)
    # Pre-mutate: t116 - differ = False - solver = True - old_items = ['shape_f', ' t54'] - old_hints = ['IJ', 'Patch']
    try:
        t116 = shape_f(t54)
    except (TypeError, AttributeError, ValueError):
        t116 = _get_safe_default(shape_f)
    # Pre-mutate: t117 - differ = False - solver = True - old_items = ['f_ofcolor', ' I', ' EIGHT'] - old_hints = ['Indices', 'Grid', 'C_']
    try:
        t117 = f_ofcolor(I, CYAN)
    except (TypeError, AttributeError, ValueError):
        t117 = _get_safe_default(f_ofcolor)
    # Pre-mutate: t118 - differ = False - solver = True - old_items = ['mapply_t', ' t56', ' t74'] - old_hints = ['Tuple', 'Callable', 'Tuple']
    try:
        t118 = mapply_t(t56, t74)
    except (TypeError, AttributeError, ValueError):
        t118 = _get_safe_default(mapply_t)
    # Pre-mutate: t119 - differ = False - solver = True - old_items = ['merge_t', ' t74'] - old_hints = ['Container', 'ContainerContainer']
    try:
        t119 = merge_t(t74)
    except (TypeError, AttributeError, ValueError):
        t119 = _get_safe_default(merge_t)
    # Pre-mutate: t120 - differ = False - solver = True - old_items = ['subgrid', ' t75', ' I'] - old_hints = ['Grid', 'Patch', 'Grid']
    try:
        t120 = subgrid(t75, I)
    except (TypeError, AttributeError, ValueError):
        t120 = _get_safe_default(subgrid)
    # Pre-mutate: t121 - differ = False - solver = True - old_items = ['chain', ' invert', ' halve', ' shape_f'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t121 = chain(invert, halve, shape_f)
    except (TypeError, AttributeError, ValueError):
        t121 = _get_safe_default(chain)
    # Pre-mutate: t122 - differ = False - solver = True - old_items = ['fill', ' I', ' TWO', ' t77'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t122 = fill(I, RED, t77)
    except (TypeError, AttributeError, ValueError):
        t122 = _get_safe_default(fill)
    # Pre-mutate: t123 - differ = False - solver = True - old_items = ['asindices', ' t78'] - old_hints = ['Indices', 'Grid']
    try:
        t123 = asindices(t78)
    except (TypeError, AttributeError, ValueError):
        t123 = _get_safe_default(asindices)
    # Pre-mutate: t124 - differ = False - solver = True - old_items = ['astuple', ' t79', ' ONE'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t124 = astuple(t79, ONE)
    except (TypeError, AttributeError, ValueError):
        t124 = _get_safe_default(astuple)
    # Pre-mutate: t125 - differ = False - solver = True - old_items = ['height_f', ' t79'] - old_hints = ['Integer', 'Indices']
    try:
        t125 = height_f(t79)
    except (TypeError, AttributeError, ValueError):
        t125 = _get_safe_default(height_f)
    # Pre-mutate: t126 - differ = False - solver = True - old_items = ['underfill', ' I', ' EIGHT', ' t108'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t126 = underfill(I, CYAN, t108)
    except (TypeError, AttributeError, ValueError):
        t126 = _get_safe_default(underfill)
    # Pre-mutate: t127 - differ = False - solver = True - old_items = ['vconcat', ' t66', ' t110'] - old_hints = ['Grid', 'Grid', 'Grid']
    try:
        t127 = vconcat(t66, t110)
    except (TypeError, AttributeError, ValueError):
        t127 = _get_safe_default(vconcat)
    o.append((127, False, '67e8384a', t127))
    # Pre-mutate: t128 - differ = True - solver = False - old_items = ['t1', ' t127'] - old_hints = None
    try:
        t128 = t1(t127)
    except (TypeError, AttributeError, ValueError):
        t128 = _get_safe_default(t1)
    # Pre-mutate: t129 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t128'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t129 = difference_tuple(t3, t128)
    except (TypeError, AttributeError, ValueError):
        t129 = _get_safe_default(difference_tuple)
    # Pre-mutate: t130 - differ = True - solver = False - old_items = ['size', ' t129'] - old_hints = ['Integer', 'Container']
    try:
        t130 = size(t129)
    except (TypeError, AttributeError, ValueError):
        t130 = _get_safe_default(size)
    # Pre-mutate: t131 - differ = True - solver = True - old_items = ['get_nth_t', ' t129', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t131 = get_nth_t(t129, F0)
    except (TypeError, AttributeError, ValueError):
        t131 = _get_safe_default(get_nth_t)
    # Pre-mutate: t132 - differ = True - solver = False - old_items = ['astuple', ' t130', ' t131'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t132 = astuple(t130, t131)
    except (TypeError, AttributeError, ValueError):
        t132 = _get_safe_default(astuple)
    s.append((132, '67e8384a', 'differ_c59e6ae069a9205b311cf67a15473d64', t132))
    # Pre-mutate: t133 - differ = True - solver = False - old_items = ['t8', ' t127'] - old_hints = None
    try:
        t133 = t8(t127)
    except (TypeError, AttributeError, ValueError):
        t133 = _get_safe_default(t8)
    # Pre-mutate: t134 - differ = True - solver = False - old_items = ['t11', ' t133'] - old_hints = None
    try:
        t134 = t11(t133)
    except (TypeError, AttributeError, ValueError):
        t134 = _get_safe_default(t11)
    # Pre-mutate: t135 - differ = True - solver = False - old_items = ['difference', ' t134', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t135 = difference(t134, t13)
    except (TypeError, AttributeError, ValueError):
        t135 = _get_safe_default(difference)
    # Pre-mutate: t136 - differ = True - solver = False - old_items = ['size', ' t135'] - old_hints = ['Integer', 'Container']
    try:
        t136 = size(t135)
    except (TypeError, AttributeError, ValueError):
        t136 = _get_safe_default(size)
    # Pre-mutate: t137 - differ = True - solver = True - old_items = ['get_nth_f', ' t135', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t137 = get_nth_f(t135, F0)
    except (TypeError, AttributeError, ValueError):
        t137 = _get_safe_default(get_nth_f)
    # Pre-mutate: t138 - differ = True - solver = False - old_items = ['astuple', ' t136', ' t137'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t138 = astuple(t136, t137)
    except (TypeError, AttributeError, ValueError):
        t138 = _get_safe_default(astuple)
    s.append((138, '67e8384a', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t138))
    # Pre-mutate: t139 - differ = True - solver = False - old_items = ['difference', ' t13', ' t134'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t139 = difference(t13, t134)
    except (TypeError, AttributeError, ValueError):
        t139 = _get_safe_default(difference)
    # Pre-mutate: t140 - differ = True - solver = False - old_items = ['size', ' t139'] - old_hints = ['Integer', 'Container']
    try:
        t140 = size(t139)
    except (TypeError, AttributeError, ValueError):
        t140 = _get_safe_default(size)
    # Pre-mutate: t141 - differ = True - solver = True - old_items = ['get_nth_f', ' t139', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t141 = get_nth_f(t139, F0)
    except (TypeError, AttributeError, ValueError):
        t141 = _get_safe_default(get_nth_f)
    # Pre-mutate: t142 - differ = True - solver = False - old_items = ['astuple', ' t140', ' t141'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t142 = astuple(t140, t141)
    except (TypeError, AttributeError, ValueError):
        t142 = _get_safe_default(astuple)
    s.append((142, '67e8384a', 'differ_f4a4346405e974b919a4b5a52f28e40c', t142))
    # Pre-mutate: t143 - differ = True - solver = False - old_items = ['difference_tuple', ' t128', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t143 = difference_tuple(t128, t3)
    except (TypeError, AttributeError, ValueError):
        t143 = _get_safe_default(difference_tuple)
    # Pre-mutate: t144 - differ = True - solver = False - old_items = ['size', ' t143'] - old_hints = ['Integer', 'Container']
    try:
        t144 = size(t143)
    except (TypeError, AttributeError, ValueError):
        t144 = _get_safe_default(size)
    # Pre-mutate: t145 - differ = True - solver = True - old_items = ['get_nth_t', ' t143', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t145 = get_nth_t(t143, F0)
    except (TypeError, AttributeError, ValueError):
        t145 = _get_safe_default(get_nth_t)
    # Pre-mutate: t146 - differ = True - solver = False - old_items = ['astuple', ' t144', ' t145'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t146 = astuple(t144, t145)
    except (TypeError, AttributeError, ValueError):
        t146 = _get_safe_default(astuple)
    s.append((146, '67e8384a', 'differ_fc9e44c913711da609a9e25126b4b296', t146))
    # Pre-mutate: t147 - differ = False - solver = True - old_items = ['compose', ' t111', ' center'] - old_hints = ['Callable', 'Callable', 'Callable']
    try:
        t147 = compose(t111, center)
    except (TypeError, AttributeError, ValueError):
        t147 = _get_safe_default(compose)
    # Pre-mutate: t148 - differ = False - solver = True - old_items = ['subtract', ' t68', ' t112'] - old_hints = ['Numerical', 'Numerical', 'Numerical']
    try:
        t148 = subtract(t68, t112)
    except (TypeError, AttributeError, ValueError):
        t148 = _get_safe_default(subtract)
    # Pre-mutate: t149 - differ = False - solver = True - old_items = ['f_ofcolor', ' I', ' t112'] - old_hints = ['Indices', 'Grid', 'C_']
    try:
        t149 = f_ofcolor(I, t112)
    except (TypeError, AttributeError, ValueError):
        t149 = _get_safe_default(f_ofcolor)
    # Pre-mutate: t150 - differ = False - solver = True - old_items = ['f_ofcolor', ' I', ' t27'] - old_hints = ['Indices', 'Grid', 'C_']
    try:
        t150 = f_ofcolor(I, t27)
    except (TypeError, AttributeError, ValueError):
        t150 = _get_safe_default(f_ofcolor)
    # Pre-mutate: t151 - differ = False - solver = True - old_items = ['matcher', ' size', ' ZERO'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t151 = matcher(size, ZERO)
    except (TypeError, AttributeError, ValueError):
        t151 = _get_safe_default(matcher)
    # Pre-mutate: t152 - differ = False - solver = True - old_items = ['compose', ' crement', ' t115'] - old_hints = ['Callable', 'Callable', 'Callable']
    try:
        t152 = compose(crement, t115)
    except (TypeError, AttributeError, ValueError):
        t152 = _get_safe_default(compose)
    # Pre-mutate: t153 - differ = False - solver = True - old_items = ['lbind', ' multiply', ' t116'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t153 = lbind(multiply, t116)
    except (TypeError, AttributeError, ValueError):
        t153 = _get_safe_default(lbind)
    # Pre-mutate: t154 - differ = False - solver = True - old_items = ['mapply_t', ' neighbors', ' t117'] - old_hints = ['Tuple', 'Callable', 'Tuple']
    try:
        t154 = mapply_t(neighbors, t117)
    except (TypeError, AttributeError, ValueError):
        t154 = _get_safe_default(mapply_t)
    # Pre-mutate: t155 - differ = False - solver = True - old_items = ['underfill', ' I', ' SIX', ' t118'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t155 = underfill(I, MAGENTA, t118)
    except (TypeError, AttributeError, ValueError):
        t155 = _get_safe_default(underfill)
    o.append((155, True, '41e4d17e', t155))
    # Pre-mutate: t156 - differ = True - solver = False - old_items = ['t1', ' t155'] - old_hints = None
    try:
        t156 = t1(t155)
    except (TypeError, AttributeError, ValueError):
        t156 = _get_safe_default(t1)
    # Pre-mutate: t157 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t156'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t157 = difference_tuple(t3, t156)
    except (TypeError, AttributeError, ValueError):
        t157 = _get_safe_default(difference_tuple)
    # Pre-mutate: t158 - differ = True - solver = False - old_items = ['size', ' t157'] - old_hints = ['Integer', 'Container']
    try:
        t158 = size(t157)
    except (TypeError, AttributeError, ValueError):
        t158 = _get_safe_default(size)
    # Pre-mutate: t159 - differ = True - solver = True - old_items = ['get_nth_t', ' t157', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t159 = get_nth_t(t157, F0)
    except (TypeError, AttributeError, ValueError):
        t159 = _get_safe_default(get_nth_t)
    # Pre-mutate: t160 - differ = True - solver = False - old_items = ['astuple', ' t158', ' t159'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t160 = astuple(t158, t159)
    except (TypeError, AttributeError, ValueError):
        t160 = _get_safe_default(astuple)
    s.append((160, '41e4d17e', 'differ_c59e6ae069a9205b311cf67a15473d64', t160))
    # Pre-mutate: t161 - differ = True - solver = False - old_items = ['t8', ' t155'] - old_hints = None
    try:
        t161 = t8(t155)
    except (TypeError, AttributeError, ValueError):
        t161 = _get_safe_default(t8)
    # Pre-mutate: t162 - differ = True - solver = False - old_items = ['t11', ' t161'] - old_hints = None
    try:
        t162 = t11(t161)
    except (TypeError, AttributeError, ValueError):
        t162 = _get_safe_default(t11)
    # Pre-mutate: t163 - differ = True - solver = False - old_items = ['difference', ' t162', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t163 = difference(t162, t13)
    except (TypeError, AttributeError, ValueError):
        t163 = _get_safe_default(difference)
    # Pre-mutate: t164 - differ = True - solver = False - old_items = ['size', ' t163'] - old_hints = ['Integer', 'Container']
    try:
        t164 = size(t163)
    except (TypeError, AttributeError, ValueError):
        t164 = _get_safe_default(size)
    # Pre-mutate: t165 - differ = True - solver = True - old_items = ['get_nth_f', ' t163', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t165 = get_nth_f(t163, F0)
    except (TypeError, AttributeError, ValueError):
        t165 = _get_safe_default(get_nth_f)
    # Pre-mutate: t166 - differ = True - solver = False - old_items = ['astuple', ' t164', ' t165'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t166 = astuple(t164, t165)
    except (TypeError, AttributeError, ValueError):
        t166 = _get_safe_default(astuple)
    s.append((166, '41e4d17e', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t166))
    # Pre-mutate: t167 - differ = True - solver = False - old_items = ['difference', ' t13', ' t162'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t167 = difference(t13, t162)
    except (TypeError, AttributeError, ValueError):
        t167 = _get_safe_default(difference)
    # Pre-mutate: t168 - differ = True - solver = False - old_items = ['size', ' t167'] - old_hints = ['Integer', 'Container']
    try:
        t168 = size(t167)
    except (TypeError, AttributeError, ValueError):
        t168 = _get_safe_default(size)
    # Pre-mutate: t169 - differ = True - solver = True - old_items = ['get_nth_f', ' t167', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t169 = get_nth_f(t167, F0)
    except (TypeError, AttributeError, ValueError):
        t169 = _get_safe_default(get_nth_f)
    # Pre-mutate: t170 - differ = True - solver = False - old_items = ['astuple', ' t168', ' t169'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t170 = astuple(t168, t169)
    except (TypeError, AttributeError, ValueError):
        t170 = _get_safe_default(astuple)
    s.append((170, '41e4d17e', 'differ_f4a4346405e974b919a4b5a52f28e40c', t170))
    # Pre-mutate: t171 - differ = True - solver = False - old_items = ['difference_tuple', ' t156', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t171 = difference_tuple(t156, t3)
    except (TypeError, AttributeError, ValueError):
        t171 = _get_safe_default(difference_tuple)
    # Pre-mutate: t172 - differ = True - solver = False - old_items = ['size', ' t171'] - old_hints = ['Integer', 'Container']
    try:
        t172 = size(t171)
    except (TypeError, AttributeError, ValueError):
        t172 = _get_safe_default(size)
    # Pre-mutate: t173 - differ = True - solver = True - old_items = ['get_nth_t', ' t171', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t173 = get_nth_t(t171, F0)
    except (TypeError, AttributeError, ValueError):
        t173 = _get_safe_default(get_nth_t)
    # Pre-mutate: t174 - differ = True - solver = False - old_items = ['astuple', ' t172', ' t173'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t174 = astuple(t172, t173)
    except (TypeError, AttributeError, ValueError):
        t174 = _get_safe_default(astuple)
    s.append((174, '41e4d17e', 'differ_fc9e44c913711da609a9e25126b4b296', t174))
    # Pre-mutate: t175 - differ = False - solver = True - old_items = ['backdrop', ' t119'] - old_hints = ['Indices', 'Patch']
    try:
        t175 = backdrop(t119)
    except (TypeError, AttributeError, ValueError):
        t175 = _get_safe_default(backdrop)
    # Pre-mutate: t176 - differ = False - solver = True - old_items = ['fork', ' shift', ' t76', ' t121'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t176 = fork(shift, t45, t121)
    except (TypeError, AttributeError, ValueError):
        t176 = _get_safe_default(fork)
    # Pre-mutate: t177 - differ = False - solver = True - old_items = ['compose', ' flip', ' t60'] - old_hints = ['Callable', 'Callable', 'Callable']
    try:
        t177 = compose(flip, t60)
    except (TypeError, AttributeError, ValueError):
        t177 = _get_safe_default(compose)
    # Pre-mutate: t178 - differ = False - solver = True - old_items = ['f_ofcolor', ' t78', ' ZERO'] - old_hints = ['Indices', 'Grid', 'C_']
    try:
        t178 = f_ofcolor(t78, BLACK)
    except (TypeError, AttributeError, ValueError):
        t178 = _get_safe_default(f_ofcolor)
    # Pre-mutate: t179 - differ = False - solver = True - old_items = ['shoot', ' t124', ' UP_RIGHT'] - old_hints = ['Indices', 'IJ', 'IJ']
    try:
        t179 = shoot(t124, UP_RIGHT)
    except (TypeError, AttributeError, ValueError):
        t179 = _get_safe_default(shoot)
    # Pre-mutate: t180 - differ = False - solver = True - old_items = ['equality', ' t125', ' ONE'] - old_hints = ['Boolean', 'Any', 'Any']
    try:
        t180 = equality(t125, ONE)
    except (TypeError, AttributeError, ValueError):
        t180 = _get_safe_default(equality)
    # Pre-mutate: t181 - differ = False - solver = True - old_items = ['hconcat', ' t126', ' t126'] - old_hints = ['Grid', 'Grid', 'Grid']
    try:
        t181 = hconcat(t126, t126)
    except (TypeError, AttributeError, ValueError):
        t181 = _get_safe_default(hconcat)
    # Pre-mutate: t182 - differ = False - solver = True - old_items = ['crop', ' I', ' TWO_BY_ZERO', ' THREE_BY_THREE'] - old_hints = ['Grid', 'Grid', 'IJ', 'IJ']
    try:
        t182 = crop(I, TWO_BY_ZERO, THREE_BY_THREE)
    except (TypeError, AttributeError, ValueError):
        t182 = _get_safe_default(crop)
    # Pre-mutate: t183 - differ = False - solver = True - old_items = ['fork', ' recolor_i', ' color', ' t147'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t183 = fork(recolor_i, color, t147)
    except (TypeError, AttributeError, ValueError):
        t183 = _get_safe_default(fork)
    # Pre-mutate: t184 - differ = False - solver = True - old_items = ['lbind', ' multiply', ' t148'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t184 = lbind(multiply, t148)
    except (TypeError, AttributeError, ValueError):
        t184 = _get_safe_default(lbind)
    # Pre-mutate: t185 - differ = False - solver = True - old_items = ['prapply', ' connect', ' t149', ' t149'] - old_hints = ['FrozenSet', 'Container', 'Container']
    try:
        t185 = prapply(connect, t149, t149)
    except (TypeError, AttributeError, ValueError):
        t185 = _get_safe_default(prapply)
    # Pre-mutate: t186 - differ = False - solver = True - old_items = ['prapply', ' connect', ' t113', ' t150'] - old_hints = ['FrozenSet', 'Container', 'Container']
    try:
        t186 = prapply(connect, t113, t150)
    except (TypeError, AttributeError, ValueError):
        t186 = _get_safe_default(prapply)
    # Pre-mutate: t187 - differ = False - solver = True - old_items = ['f_ofcolor', ' I', ' FIVE'] - old_hints = ['Indices', 'Grid', 'C_']
    try:
        t187 = f_ofcolor(I, GRAY)
    except (TypeError, AttributeError, ValueError):
        t187 = _get_safe_default(f_ofcolor)
    # Pre-mutate: t188 - differ = False - solver = True - old_items = ['fork', ' add', ' t53', ' t152'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t188 = fork(add, t53, t152)
    except (TypeError, AttributeError, ValueError):
        t188 = _get_safe_default(fork)
    # Pre-mutate: t189 - differ = False - solver = True - old_items = ['lbind', ' mapply', ' neighbors'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t189 = lbind(mapply, neighbors)
    except (TypeError, AttributeError, ValueError):
        t189 = _get_safe_default(lbind)
    # Pre-mutate: t190 - differ = False - solver = True - old_items = ['fill', ' t74', ' FOUR', ' t154'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    # GPU Batch Pattern: Sample processing (t190-t193)
    t190, t191, t192, t193 = batch_process_samples_gpu(S)
    t194 = dedupe(t192)
    t195 = dedupe(t193)
    t196 = difference_tuple(t195, t194)
    t197 = get_nth_t(t196, F2)
    try:
        t198 = fill(t74, t197, t154)
    except (TypeError, AttributeError, ValueError):
        t198 = _get_safe_default(fill)
    # Pre-mutate: t199 - differ = False - solver = True - old_items = ['fill', ' I', ' t57', ' t175'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t199 = fill(I, t57, t175)
    except (TypeError, AttributeError, ValueError):
        t199 = _get_safe_default(fill)
    # Pre-mutate: t200 - differ = False - solver = True - old_items = ['rbind', ' get_arg_rank', ' L1'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t200 = rbind(get_arg_rank, L1)
    except (TypeError, AttributeError, ValueError):
        t200 = _get_safe_default(rbind)
    # Pre-mutate: t201 - differ = False - solver = True - old_items = ['compose', ' toindices', ' t176'] - old_hints = ['Callable', 'Callable', 'Callable']
    try:
        t201 = compose(toindices, t176)
    except (TypeError, AttributeError, ValueError):
        t201 = _get_safe_default(compose)
    # Pre-mutate: t202 - differ = False - solver = True - old_items = ['mfilter_f', ' t43', ' t177'] - old_hints = ['FrozenSet', 'FrozenSet', 'Callable']
    try:
        t202 = mfilter_f(t43, t177)
    except (TypeError, AttributeError, ValueError):
        t202 = _get_safe_default(mfilter_f)
    # Pre-mutate: t203 - differ = False - solver = True - old_items = ['difference_t', ' t123', ' t178'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t203 = difference_t(t123, t178)
    except (TypeError, AttributeError, ValueError):
        t203 = _get_safe_default(difference_t)
    # Pre-mutate: t204 - differ = False - solver = True - old_items = ['fill', ' I', ' TWO', ' t179'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    # GPU Batch Pattern: Sample processing (t204-t207)
    t204, t205, t206, t207 = batch_process_samples_gpu(S)
    t208 = dedupe(t206)
    t209 = dedupe(t207)
    t210 = difference_tuple(t209, t208)
    t211 = get_nth_t(t210, F0)
    try:
        t212 = fill(I, t211, t179)
    except (TypeError, AttributeError, ValueError):
        t212 = _get_safe_default(fill)
    # Pre-mutate: t213 - differ = False - solver = True - old_items = ['branch', ' t180', ' ZERO_BY_TWO', ' TWO_BY_ZERO'] - old_hints = ['Any', 'Boolean', 'Any', 'Any']
    try:
        t213 = branch(t180, ZERO_BY_TWO, TWO_BY_ZERO)
    except (TypeError, AttributeError, ValueError):
        t213 = _get_safe_default(branch)
    # Pre-mutate: t214 - differ = False - solver = True - old_items = ['vconcat', ' t181', ' t181'] - old_hints = ['Grid', 'Grid', 'Grid']
    try:
        t214 = vconcat(t181, t181)
    except (TypeError, AttributeError, ValueError):
        t214 = _get_safe_default(vconcat)
    o.append((214, False, 'f5b8619d', t214))
    # Pre-mutate: t215 - differ = True - solver = False - old_items = ['t1', ' t214'] - old_hints = None
    try:
        t215 = t1(t214)
    except (TypeError, AttributeError, ValueError):
        t215 = _get_safe_default(t1)
    # Pre-mutate: t216 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t215'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t216 = difference_tuple(t3, t215)
    except (TypeError, AttributeError, ValueError):
        t216 = _get_safe_default(difference_tuple)
    # Pre-mutate: t217 - differ = True - solver = False - old_items = ['size', ' t216'] - old_hints = ['Integer', 'Container']
    try:
        t217 = size(t216)
    except (TypeError, AttributeError, ValueError):
        t217 = _get_safe_default(size)
    # Pre-mutate: t218 - differ = True - solver = True - old_items = ['get_nth_t', ' t216', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t218 = get_nth_t(t216, F0)
    except (TypeError, AttributeError, ValueError):
        t218 = _get_safe_default(get_nth_t)
    # Pre-mutate: t219 - differ = True - solver = False - old_items = ['astuple', ' t217', ' t218'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t219 = astuple(t217, t218)
    except (TypeError, AttributeError, ValueError):
        t219 = _get_safe_default(astuple)
    s.append((219, 'f5b8619d', 'differ_c59e6ae069a9205b311cf67a15473d64', t219))
    # Pre-mutate: t220 - differ = True - solver = False - old_items = ['t8', ' t214'] - old_hints = None
    try:
        t220 = t8(t214)
    except (TypeError, AttributeError, ValueError):
        t220 = _get_safe_default(t8)
    # Pre-mutate: t221 - differ = True - solver = False - old_items = ['t11', ' t220'] - old_hints = None
    try:
        t221 = t11(t220)
    except (TypeError, AttributeError, ValueError):
        t221 = _get_safe_default(t11)
    # Pre-mutate: t222 - differ = True - solver = False - old_items = ['difference', ' t221', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t222 = difference(t221, t13)
    except (TypeError, AttributeError, ValueError):
        t222 = _get_safe_default(difference)
    # Pre-mutate: t223 - differ = True - solver = False - old_items = ['size', ' t222'] - old_hints = ['Integer', 'Container']
    try:
        t223 = size(t222)
    except (TypeError, AttributeError, ValueError):
        t223 = _get_safe_default(size)
    # Pre-mutate: t224 - differ = True - solver = True - old_items = ['get_nth_f', ' t222', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t224 = get_nth_f(t222, F0)
    except (TypeError, AttributeError, ValueError):
        t224 = _get_safe_default(get_nth_f)
    # Pre-mutate: t225 - differ = True - solver = False - old_items = ['astuple', ' t223', ' t224'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t225 = astuple(t223, t224)
    except (TypeError, AttributeError, ValueError):
        t225 = _get_safe_default(astuple)
    s.append((225, 'f5b8619d', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t225))
    # Pre-mutate: t226 - differ = True - solver = False - old_items = ['difference', ' t13', ' t221'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t226 = difference(t13, t221)
    except (TypeError, AttributeError, ValueError):
        t226 = _get_safe_default(difference)
    # Pre-mutate: t227 - differ = True - solver = False - old_items = ['size', ' t226'] - old_hints = ['Integer', 'Container']
    try:
        t227 = size(t226)
    except (TypeError, AttributeError, ValueError):
        t227 = _get_safe_default(size)
    # Pre-mutate: t228 - differ = True - solver = True - old_items = ['get_nth_f', ' t226', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t228 = get_nth_f(t226, F0)
    except (TypeError, AttributeError, ValueError):
        t228 = _get_safe_default(get_nth_f)
    # Pre-mutate: t229 - differ = True - solver = False - old_items = ['astuple', ' t227', ' t228'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t229 = astuple(t227, t228)
    except (TypeError, AttributeError, ValueError):
        t229 = _get_safe_default(astuple)
    s.append((229, 'f5b8619d', 'differ_f4a4346405e974b919a4b5a52f28e40c', t229))
    # Pre-mutate: t230 - differ = True - solver = False - old_items = ['difference_tuple', ' t215', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t230 = difference_tuple(t215, t3)
    except (TypeError, AttributeError, ValueError):
        t230 = _get_safe_default(difference_tuple)
    # Pre-mutate: t231 - differ = True - solver = False - old_items = ['size', ' t230'] - old_hints = ['Integer', 'Container']
    try:
        t231 = size(t230)
    except (TypeError, AttributeError, ValueError):
        t231 = _get_safe_default(size)
    # Pre-mutate: t232 - differ = True - solver = True - old_items = ['get_nth_t', ' t230', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t232 = get_nth_t(t230, F0)
    except (TypeError, AttributeError, ValueError):
        t232 = _get_safe_default(get_nth_t)
    # Pre-mutate: t233 - differ = True - solver = False - old_items = ['astuple', ' t231', ' t232'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t233 = astuple(t231, t232)
    except (TypeError, AttributeError, ValueError):
        t233 = _get_safe_default(astuple)
    s.append((233, 'f5b8619d', 'differ_fc9e44c913711da609a9e25126b4b296', t233))
    # Pre-mutate: t234 - differ = False - solver = True - old_items = ['astuple', ' t126', ' t182'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t234 = astuple(t126, t182)
    except (TypeError, AttributeError, ValueError):
        t234 = _get_safe_default(astuple)
    # Pre-mutate: t235 - differ = False - solver = True - old_items = ['interval', ' ZERO', ' NINE', ' ONE'] - old_hints = ['Tuple', 'Integer', 'Integer', 'Integer']
    try:
        t235 = interval(ZERO, NINE, ONE)
    except (TypeError, AttributeError, ValueError):
        t235 = _get_safe_default(interval)
    # Pre-mutate: t236 - differ = False - solver = True - old_items = ['fork', ' either', ' hline_i', ' vline_i'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t236 = fork(either, hline_i, vline_i)
    except (TypeError, AttributeError, ValueError):
        t236 = _get_safe_default(fork)
    # Pre-mutate: t237 - differ = False - solver = True - old_items = ['fork', ' either', ' vline_i', ' hline_i'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t237 = fork(either, vline_i, hline_i)
    except (TypeError, AttributeError, ValueError):
        t237 = _get_safe_default(fork)
    # Pre-mutate: t238 - differ = False - solver = True - old_items = ['rbind', ' intersection', ' t187'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t238 = rbind(intersection, t187)
    except (TypeError, AttributeError, ValueError):
        t238 = _get_safe_default(rbind)
    # Pre-mutate: t239 - differ = False - solver = True - old_items = ['fork', ' connect', ' t53', ' t188'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t239 = fork(connect, t53, t188)
    except (TypeError, AttributeError, ValueError):
        t239 = _get_safe_default(fork)
    # Pre-mutate: t240 - differ = False - solver = True - old_items = ['power', ' t189', ' TWO'] - old_hints = ['Callable', 'Callable', 'Integer']
    try:
        t240 = power(t189, TWO)
    except (TypeError, AttributeError, ValueError):
        t240 = _get_safe_default(power)
    # Pre-mutate: t241 - differ = False - solver = True - old_items = ['replace', ' t41', ' t57', ' ZERO'] - old_hints = ['Grid', 'Grid', 'C_', 'C_']
    try:
        t241 = replace(t41, t57, BLACK)
    except (TypeError, AttributeError, ValueError):
        t241 = _get_safe_default(replace)
    # Pre-mutate: t242 - differ = False - solver = True - old_items = ['normalize_o', ' t75'] - old_hints = ['Object', 'Object']
    try:
        t242 = normalize_o(t75)
    except (TypeError, AttributeError, ValueError):
        t242 = _get_safe_default(normalize_o)
    # Pre-mutate: t243 - differ = False - solver = True - old_items = ['fork', ' combine', ' hfrontier', ' vfrontier'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t243 = fork(width_o, hfrontier, vfrontier)
    except (TypeError, AttributeError, ValueError):
        t243 = _get_safe_default(fork)
    # Pre-mutate: t244 - differ = False - solver = True - old_items = ['fill', ' t122', ' ONE', ' t202'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t244 = fill(t122, BLUE, t202)
    except (TypeError, AttributeError, ValueError):
        t244 = _get_safe_default(fill)
    o.append((244, True, 'd2abd087', t244))
    # Pre-mutate: t245 - differ = True - solver = False - old_items = ['t1', ' t244'] - old_hints = None
    try:
        t245 = t1(t244)
    except (TypeError, AttributeError, ValueError):
        t245 = _get_safe_default(t1)
    # Pre-mutate: t246 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t245'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t246 = difference_tuple(t3, t245)
    except (TypeError, AttributeError, ValueError):
        t246 = _get_safe_default(difference_tuple)
    # Pre-mutate: t247 - differ = True - solver = False - old_items = ['size', ' t246'] - old_hints = ['Integer', 'Container']
    try:
        t247 = size(t246)
    except (TypeError, AttributeError, ValueError):
        t247 = _get_safe_default(size)
    # Pre-mutate: t248 - differ = True - solver = True - old_items = ['get_nth_t', ' t246', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t248 = get_nth_t(t246, F0)
    except (TypeError, AttributeError, ValueError):
        t248 = _get_safe_default(get_nth_t)
    # Pre-mutate: t249 - differ = True - solver = False - old_items = ['astuple', ' t247', ' t248'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t249 = astuple(t247, t248)
    except (TypeError, AttributeError, ValueError):
        t249 = _get_safe_default(astuple)
    s.append((249, 'd2abd087', 'differ_c59e6ae069a9205b311cf67a15473d64', t249))
    # Pre-mutate: t250 - differ = True - solver = False - old_items = ['t8', ' t244'] - old_hints = None
    try:
        t250 = t8(t244)
    except (TypeError, AttributeError, ValueError):
        t250 = _get_safe_default(t8)
    # Pre-mutate: t251 - differ = True - solver = False - old_items = ['t11', ' t250'] - old_hints = None
    try:
        t251 = t11(t250)
    except (TypeError, AttributeError, ValueError):
        t251 = _get_safe_default(t11)
    # Pre-mutate: t252 - differ = True - solver = False - old_items = ['difference', ' t251', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t252 = difference(t251, t13)
    except (TypeError, AttributeError, ValueError):
        t252 = _get_safe_default(difference)
    # Pre-mutate: t253 - differ = True - solver = False - old_items = ['size', ' t252'] - old_hints = ['Integer', 'Container']
    try:
        t253 = size(t252)
    except (TypeError, AttributeError, ValueError):
        t253 = _get_safe_default(size)
    # Pre-mutate: t254 - differ = True - solver = True - old_items = ['get_nth_f', ' t252', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t254 = get_nth_f(t252, F0)
    except (TypeError, AttributeError, ValueError):
        t254 = _get_safe_default(get_nth_f)
    # Pre-mutate: t255 - differ = True - solver = False - old_items = ['astuple', ' t253', ' t254'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t255 = astuple(t253, t254)
    except (TypeError, AttributeError, ValueError):
        t255 = _get_safe_default(astuple)
    s.append((255, 'd2abd087', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t255))
    # Pre-mutate: t256 - differ = True - solver = False - old_items = ['difference', ' t13', ' t251'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t256 = difference(t13, t251)
    except (TypeError, AttributeError, ValueError):
        t256 = _get_safe_default(difference)
    # Pre-mutate: t257 - differ = True - solver = False - old_items = ['size', ' t256'] - old_hints = ['Integer', 'Container']
    try:
        t257 = size(t256)
    except (TypeError, AttributeError, ValueError):
        t257 = _get_safe_default(size)
    # Pre-mutate: t258 - differ = True - solver = True - old_items = ['get_nth_f', ' t256', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t258 = get_nth_f(t256, F0)
    except (TypeError, AttributeError, ValueError):
        t258 = _get_safe_default(get_nth_f)
    # Pre-mutate: t259 - differ = True - solver = False - old_items = ['astuple', ' t257', ' t258'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t259 = astuple(t257, t258)
    except (TypeError, AttributeError, ValueError):
        t259 = _get_safe_default(astuple)
    s.append((259, 'd2abd087', 'differ_f4a4346405e974b919a4b5a52f28e40c', t259))
    # Pre-mutate: t260 - differ = True - solver = False - old_items = ['difference_tuple', ' t245', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t260 = difference_tuple(t245, t3)
    except (TypeError, AttributeError, ValueError):
        t260 = _get_safe_default(difference_tuple)
    # Pre-mutate: t261 - differ = True - solver = False - old_items = ['size', ' t260'] - old_hints = ['Integer', 'Container']
    try:
        t261 = size(t260)
    except (TypeError, AttributeError, ValueError):
        t261 = _get_safe_default(size)
    # Pre-mutate: t262 - differ = True - solver = True - old_items = ['get_nth_t', ' t260', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t262 = get_nth_t(t260, F0)
    except (TypeError, AttributeError, ValueError):
        t262 = _get_safe_default(get_nth_t)
    # Pre-mutate: t263 - differ = True - solver = False - old_items = ['astuple', ' t261', ' t262'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t263 = astuple(t261, t262)
    except (TypeError, AttributeError, ValueError):
        t263 = _get_safe_default(astuple)
    s.append((263, 'd2abd087', 'differ_fc9e44c913711da609a9e25126b4b296', t263))
    # Pre-mutate: t264 - differ = False - solver = True - old_items = ['normalize', ' t203'] - old_hints = ['Indices', 'Indices']
    try:
        t264 = normalize(t203)
    except (TypeError, AttributeError, ValueError):
        t264 = _get_safe_default(normalize)
    # Pre-mutate: t265 - differ = False - solver = True - old_items = ['astuple', ' t62', ' ONE'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t265 = astuple(t62, ONE)
    except (TypeError, AttributeError, ValueError):
        t265 = _get_safe_default(astuple)
    # Pre-mutate: t266 - differ = False - solver = True - old_items = ['move', ' I', ' t63', ' t213'] - old_hints = ['Grid', 'Grid', 'Object', 'IJ']
    try:
        t266 = move(I, t63, t213)
    except (TypeError, AttributeError, ValueError):
        t266 = _get_safe_default(move)
    o.append((266, False, '5168d44c', t266))
    # Pre-mutate: t267 - differ = True - solver = False - old_items = ['t1', ' t266'] - old_hints = None
    try:
        t267 = t1(t266)
    except (TypeError, AttributeError, ValueError):
        t267 = _get_safe_default(t1)
    # Pre-mutate: t268 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t267'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t268 = difference_tuple(t3, t267)
    except (TypeError, AttributeError, ValueError):
        t268 = _get_safe_default(difference_tuple)
    # Pre-mutate: t269 - differ = True - solver = False - old_items = ['size', ' t268'] - old_hints = ['Integer', 'Container']
    try:
        t269 = size(t268)
    except (TypeError, AttributeError, ValueError):
        t269 = _get_safe_default(size)
    # Pre-mutate: t270 - differ = True - solver = True - old_items = ['get_nth_t', ' t268', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t270 = get_nth_t(t268, F0)
    except (TypeError, AttributeError, ValueError):
        t270 = _get_safe_default(get_nth_t)
    # Pre-mutate: t271 - differ = True - solver = False - old_items = ['astuple', ' t269', ' t270'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t271 = astuple(t269, t270)
    except (TypeError, AttributeError, ValueError):
        t271 = _get_safe_default(astuple)
    s.append((271, '5168d44c', 'differ_c59e6ae069a9205b311cf67a15473d64', t271))
    # Pre-mutate: t272 - differ = True - solver = False - old_items = ['t8', ' t266'] - old_hints = None
    try:
        t272 = t8(t266)
    except (TypeError, AttributeError, ValueError):
        t272 = _get_safe_default(t8)
    # Pre-mutate: t273 - differ = True - solver = False - old_items = ['t11', ' t272'] - old_hints = None
    try:
        t273 = t11(t272)
    except (TypeError, AttributeError, ValueError):
        t273 = _get_safe_default(t11)
    # Pre-mutate: t274 - differ = True - solver = False - old_items = ['difference', ' t273', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t274 = difference(t273, t13)
    except (TypeError, AttributeError, ValueError):
        t274 = _get_safe_default(difference)
    # Pre-mutate: t275 - differ = True - solver = False - old_items = ['size', ' t274'] - old_hints = ['Integer', 'Container']
    try:
        t275 = size(t274)
    except (TypeError, AttributeError, ValueError):
        t275 = _get_safe_default(size)
    # Pre-mutate: t276 - differ = True - solver = True - old_items = ['get_nth_f', ' t274', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t276 = get_nth_f(t274, F0)
    except (TypeError, AttributeError, ValueError):
        t276 = _get_safe_default(get_nth_f)
    # Pre-mutate: t277 - differ = True - solver = False - old_items = ['astuple', ' t275', ' t276'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t277 = astuple(t275, t276)
    except (TypeError, AttributeError, ValueError):
        t277 = _get_safe_default(astuple)
    s.append((277, '5168d44c', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t277))
    # Pre-mutate: t278 - differ = True - solver = False - old_items = ['difference', ' t13', ' t273'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t278 = difference(t13, t273)
    except (TypeError, AttributeError, ValueError):
        t278 = _get_safe_default(difference)
    # Pre-mutate: t279 - differ = True - solver = False - old_items = ['size', ' t278'] - old_hints = ['Integer', 'Container']
    try:
        t279 = size(t278)
    except (TypeError, AttributeError, ValueError):
        t279 = _get_safe_default(size)
    # Pre-mutate: t280 - differ = True - solver = True - old_items = ['get_nth_f', ' t278', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t280 = get_nth_f(t278, F0)
    except (TypeError, AttributeError, ValueError):
        t280 = _get_safe_default(get_nth_f)
    # Pre-mutate: t281 - differ = True - solver = False - old_items = ['astuple', ' t279', ' t280'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t281 = astuple(t279, t280)
    except (TypeError, AttributeError, ValueError):
        t281 = _get_safe_default(astuple)
    s.append((281, '5168d44c', 'differ_f4a4346405e974b919a4b5a52f28e40c', t281))
    # Pre-mutate: t282 - differ = True - solver = False - old_items = ['difference_tuple', ' t267', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t282 = difference_tuple(t267, t3)
    except (TypeError, AttributeError, ValueError):
        t282 = _get_safe_default(difference_tuple)
    # Pre-mutate: t283 - differ = True - solver = False - old_items = ['size', ' t282'] - old_hints = ['Integer', 'Container']
    try:
        t283 = size(t282)
    except (TypeError, AttributeError, ValueError):
        t283 = _get_safe_default(size)
    # Pre-mutate: t284 - differ = True - solver = True - old_items = ['get_nth_t', ' t282', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t284 = get_nth_t(t282, F0)
    except (TypeError, AttributeError, ValueError):
        t284 = _get_safe_default(get_nth_t)
    # Pre-mutate: t285 - differ = True - solver = False - old_items = ['astuple', ' t283', ' t284'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t285 = astuple(t283, t284)
    except (TypeError, AttributeError, ValueError):
        t285 = _get_safe_default(astuple)
    s.append((285, '5168d44c', 'differ_fc9e44c913711da609a9e25126b4b296', t285))
    # Pre-mutate: t286 - differ = False - solver = True - old_items = ['tojvec', ' FOUR'] - old_hints = ['IJ', 'Integer']
    try:
        t286 = tojvec(FOUR)
    except (TypeError, AttributeError, ValueError):
        t286 = _get_safe_default(tojvec)
    # Pre-mutate: t287 - differ = False - solver = True - old_items = ['mapply_t', ' t183', ' t234'] - old_hints = ['Tuple', 'Callable', 'Tuple']
    try:
        t287 = mapply_t(t183, t234)
    except (TypeError, AttributeError, ValueError):
        t287 = _get_safe_default(mapply_t)
    # Pre-mutate: t288 - differ = False - solver = True - old_items = ['apply', ' t184', ' t235'] - old_hints = ['Container', 'Callable', 'Container']
    try:
        t288 = apply(t184, t235)
    except (TypeError, AttributeError, ValueError):
        t288 = _get_safe_default(apply)
    # Pre-mutate: t289 - differ = False - solver = True - old_items = ['box', ' t51'] - old_hints = ['Indices', 'Patch']
    try:
        t289 = box(t51)
    except (TypeError, AttributeError, ValueError):
        t289 = _get_safe_default(box)
    # Pre-mutate: t290 - differ = False - solver = True - old_items = ['mfilter_f', ' t186', ' t237'] - old_hints = ['FrozenSet', 'FrozenSet', 'Callable']
    try:
        t290 = mfilter_f(t186, t237)
    except (TypeError, AttributeError, ValueError):
        t290 = _get_safe_default(mfilter_f)
    # Pre-mutate: t291 - differ = False - solver = True - old_items = ['lbind', ' mapply', ' dneighbors'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t291 = lbind(mapply, dneighbors)
    except (TypeError, AttributeError, ValueError):
        t291 = _get_safe_default(lbind)
    # Pre-mutate: t292 - differ = False - solver = True - old_items = ['neighbors', ' ORIGIN'] - old_hints = ['Indices', 'IJ']
    try:
        t292 = neighbors(ORIGIN)
    except (TypeError, AttributeError, ValueError):
        t292 = _get_safe_default(neighbors)
    # Pre-mutate: t293 - differ = False - solver = True - old_items = ['mapply_t', ' neighbors', ' t240'] - old_hints = ['Tuple', 'Callable', 'Tuple']
    try:
        t293 = mapply_t(neighbors, t240)
    except (TypeError, AttributeError, ValueError):
        t293 = _get_safe_default(mapply_t)
    # Pre-mutate: t294 - differ = False - solver = True - old_items = ['get_color_rank_t', ' t241', ' L1'] - old_hints = ['C_', 'Grid', 'FL']
    try:
        t294 = get_color_rank_t(t241, L1)
    except (TypeError, AttributeError, ValueError):
        t294 = _get_safe_default(get_color_rank_t)
    # Pre-mutate: t295 - differ = False - solver = True - old_items = ['lbind', ' t200', ' t242'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t295 = remove_t(t200, t242)
    except (TypeError, AttributeError, ValueError):
        t295 = _get_safe_default(remove_t)
    # Pre-mutate: t296 - differ = False - solver = True - old_items = ['lbind', ' mapply', ' t243'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t296 = lbind(mapply, t243)
    except (TypeError, AttributeError, ValueError):
        t296 = _get_safe_default(lbind)
    # Pre-mutate: t297 - differ = False - solver = True - old_items = ['matcher', ' t61', ' t264'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t297 = matcher(t61, t264)
    except (TypeError, AttributeError, ValueError):
        t297 = _get_safe_default(matcher)
    # Pre-mutate: t298 - differ = False - solver = True - old_items = ['shoot', ' t265', ' RIGHT'] - old_hints = ['Indices', 'IJ', 'IJ']
    try:
        t298 = shoot(t265, RIGHT)
    except (TypeError, AttributeError, ValueError):
        t298 = _get_safe_default(shoot)
    # Pre-mutate: t299 - differ = False - solver = True - old_items = ['crop', ' I', ' t286', ' THREE_BY_THREE'] - old_hints = ['Grid', 'Grid', 'IJ', 'IJ']
    try:
        t299 = crop(I, t286, THREE_BY_THREE)
    except (TypeError, AttributeError, ValueError):
        t299 = _get_safe_default(crop)
    # Pre-mutate: t300 - differ = False - solver = True - old_items = ['paint', ' t67', ' t287'] - old_hints = ['Grid', 'Grid', 'Object']
    try:
        t300 = paint(t67, t287)
    except (TypeError, AttributeError, ValueError):
        t300 = _get_safe_default(paint)
    o.append((300, False, 'd13f3404', t300))
    # Pre-mutate: t301 - differ = True - solver = False - old_items = ['t1', ' t300'] - old_hints = None
    try:
        t301 = t1(t300)
    except (TypeError, AttributeError, ValueError):
        t301 = _get_safe_default(t1)
    # Pre-mutate: t302 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t301'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t302 = difference_tuple(t3, t301)
    except (TypeError, AttributeError, ValueError):
        t302 = _get_safe_default(difference_tuple)
    # Pre-mutate: t303 - differ = True - solver = False - old_items = ['size', ' t302'] - old_hints = ['Integer', 'Container']
    try:
        t303 = size(t302)
    except (TypeError, AttributeError, ValueError):
        t303 = _get_safe_default(size)
    # Pre-mutate: t304 - differ = True - solver = True - old_items = ['get_nth_t', ' t302', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t304 = get_nth_t(t302, F0)
    except (TypeError, AttributeError, ValueError):
        t304 = _get_safe_default(get_nth_t)
    # Pre-mutate: t305 - differ = True - solver = False - old_items = ['astuple', ' t303', ' t304'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t305 = astuple(t303, t304)
    except (TypeError, AttributeError, ValueError):
        t305 = _get_safe_default(astuple)
    s.append((305, 'd13f3404', 'differ_c59e6ae069a9205b311cf67a15473d64', t305))
    # Pre-mutate: t306 - differ = True - solver = False - old_items = ['t8', ' t300'] - old_hints = None
    try:
        t306 = t8(t300)
    except (TypeError, AttributeError, ValueError):
        t306 = _get_safe_default(t8)
    # Pre-mutate: t307 - differ = True - solver = False - old_items = ['t11', ' t306'] - old_hints = None
    try:
        t307 = t11(t306)
    except (TypeError, AttributeError, ValueError):
        t307 = _get_safe_default(t11)
    # Pre-mutate: t308 - differ = True - solver = False - old_items = ['difference', ' t307', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t308 = difference(t307, t13)
    except (TypeError, AttributeError, ValueError):
        t308 = _get_safe_default(difference)
    # Pre-mutate: t309 - differ = True - solver = False - old_items = ['size', ' t308'] - old_hints = ['Integer', 'Container']
    try:
        t309 = size(t308)
    except (TypeError, AttributeError, ValueError):
        t309 = _get_safe_default(size)
    # Pre-mutate: t310 - differ = True - solver = True - old_items = ['get_nth_f', ' t308', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t310 = get_nth_f(t308, F0)
    except (TypeError, AttributeError, ValueError):
        t310 = _get_safe_default(get_nth_f)
    # Pre-mutate: t311 - differ = True - solver = False - old_items = ['astuple', ' t309', ' t310'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t311 = astuple(t309, t310)
    except (TypeError, AttributeError, ValueError):
        t311 = _get_safe_default(astuple)
    s.append((311, 'd13f3404', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t311))
    # Pre-mutate: t312 - differ = True - solver = False - old_items = ['difference', ' t13', ' t307'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t312 = difference(t13, t307)
    except (TypeError, AttributeError, ValueError):
        t312 = _get_safe_default(difference)
    # Pre-mutate: t313 - differ = True - solver = False - old_items = ['size', ' t312'] - old_hints = ['Integer', 'Container']
    try:
        t313 = size(t312)
    except (TypeError, AttributeError, ValueError):
        t313 = _get_safe_default(size)
    # Pre-mutate: t314 - differ = True - solver = True - old_items = ['get_nth_f', ' t312', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t314 = get_nth_f(t312, F0)
    except (TypeError, AttributeError, ValueError):
        t314 = _get_safe_default(get_nth_f)
    # Pre-mutate: t315 - differ = True - solver = False - old_items = ['astuple', ' t313', ' t314'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t315 = astuple(t313, t314)
    except (TypeError, AttributeError, ValueError):
        t315 = _get_safe_default(astuple)
    s.append((315, 'd13f3404', 'differ_f4a4346405e974b919a4b5a52f28e40c', t315))
    # Pre-mutate: t316 - differ = True - solver = False - old_items = ['difference_tuple', ' t301', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t316 = difference_tuple(t301, t3)
    except (TypeError, AttributeError, ValueError):
        t316 = _get_safe_default(difference_tuple)
    # Pre-mutate: t317 - differ = True - solver = False - old_items = ['size', ' t316'] - old_hints = ['Integer', 'Container']
    try:
        t317 = size(t316)
    except (TypeError, AttributeError, ValueError):
        t317 = _get_safe_default(size)
    # Pre-mutate: t318 - differ = True - solver = True - old_items = ['get_nth_t', ' t316', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t318 = get_nth_t(t316, F0)
    except (TypeError, AttributeError, ValueError):
        t318 = _get_safe_default(get_nth_t)
    # Pre-mutate: t319 - differ = True - solver = False - old_items = ['astuple', ' t317', ' t318'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t319 = astuple(t317, t318)
    except (TypeError, AttributeError, ValueError):
        t319 = _get_safe_default(astuple)
    s.append((319, 'd13f3404', 'differ_fc9e44c913711da609a9e25126b4b296', t319))
    # Pre-mutate: t320 - differ = False - solver = True - old_items = ['multiply', ' NEG_ONE', ' NINE'] - old_hints = ['Numerical', 'Numerical', 'Numerical']
    try:
        t320 = multiply(NEG_ONE, NINE)
    except (TypeError, AttributeError, ValueError):
        t320 = _get_safe_default(multiply)
    # Pre-mutate: t321 - differ = False - solver = True - old_items = ['rbind', ' difference', ' t289'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t321 = rbind(difference, t289)
    except (TypeError, AttributeError, ValueError):
        t321 = _get_safe_default(rbind)
    # Pre-mutate: t322 - differ = False - solver = True - old_items = ['underfill', ' I', ' t27', ' t290'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t322 = underfill(I, t27, t290)
    except (TypeError, AttributeError, ValueError):
        t322 = _get_safe_default(underfill)
    o.append((322, False, '2c608aff', t322))
    # Pre-mutate: t323 - differ = True - solver = False - old_items = ['t1', ' t322'] - old_hints = None
    try:
        t323 = t1(t322)
    except (TypeError, AttributeError, ValueError):
        t323 = _get_safe_default(t1)
    # Pre-mutate: t324 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t323'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t324 = difference_tuple(t3, t323)
    except (TypeError, AttributeError, ValueError):
        t324 = _get_safe_default(difference_tuple)
    # Pre-mutate: t325 - differ = True - solver = False - old_items = ['size', ' t324'] - old_hints = ['Integer', 'Container']
    try:
        t325 = size(t324)
    except (TypeError, AttributeError, ValueError):
        t325 = _get_safe_default(size)
    # Pre-mutate: t326 - differ = True - solver = True - old_items = ['get_nth_t', ' t324', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t326 = get_nth_t(t324, F0)
    except (TypeError, AttributeError, ValueError):
        t326 = _get_safe_default(get_nth_t)
    # Pre-mutate: t327 - differ = True - solver = False - old_items = ['astuple', ' t325', ' t326'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t327 = astuple(t325, t326)
    except (TypeError, AttributeError, ValueError):
        t327 = _get_safe_default(astuple)
    s.append((327, '2c608aff', 'differ_c59e6ae069a9205b311cf67a15473d64', t327))
    # Pre-mutate: t328 - differ = True - solver = False - old_items = ['t8', ' t322'] - old_hints = None
    try:
        t328 = t8(t322)
    except (TypeError, AttributeError, ValueError):
        t328 = _get_safe_default(t8)
    # Pre-mutate: t329 - differ = True - solver = False - old_items = ['t11', ' t328'] - old_hints = None
    try:
        t329 = t11(t328)
    except (TypeError, AttributeError, ValueError):
        t329 = _get_safe_default(t11)
    # Pre-mutate: t330 - differ = True - solver = False - old_items = ['difference', ' t329', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t330 = difference(t329, t13)
    except (TypeError, AttributeError, ValueError):
        t330 = _get_safe_default(difference)
    # Pre-mutate: t331 - differ = True - solver = False - old_items = ['size', ' t330'] - old_hints = ['Integer', 'Container']
    try:
        t331 = size(t330)
    except (TypeError, AttributeError, ValueError):
        t331 = _get_safe_default(size)
    # Pre-mutate: t332 - differ = True - solver = True - old_items = ['get_nth_f', ' t330', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t332 = get_nth_f(t330, F0)
    except (TypeError, AttributeError, ValueError):
        t332 = _get_safe_default(get_nth_f)
    # Pre-mutate: t333 - differ = True - solver = False - old_items = ['astuple', ' t331', ' t332'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t333 = astuple(t331, t332)
    except (TypeError, AttributeError, ValueError):
        t333 = _get_safe_default(astuple)
    s.append((333, '2c608aff', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t333))
    # Pre-mutate: t334 - differ = True - solver = False - old_items = ['difference', ' t13', ' t329'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t334 = difference(t13, t329)
    except (TypeError, AttributeError, ValueError):
        t334 = _get_safe_default(difference)
    # Pre-mutate: t335 - differ = True - solver = False - old_items = ['size', ' t334'] - old_hints = ['Integer', 'Container']
    try:
        t335 = size(t334)
    except (TypeError, AttributeError, ValueError):
        t335 = _get_safe_default(size)
    # Pre-mutate: t336 - differ = True - solver = True - old_items = ['get_nth_f', ' t334', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t336 = get_nth_f(t334, F0)
    except (TypeError, AttributeError, ValueError):
        t336 = _get_safe_default(get_nth_f)
    # Pre-mutate: t337 - differ = True - solver = False - old_items = ['astuple', ' t335', ' t336'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t337 = astuple(t335, t336)
    except (TypeError, AttributeError, ValueError):
        t337 = _get_safe_default(astuple)
    s.append((337, '2c608aff', 'differ_f4a4346405e974b919a4b5a52f28e40c', t337))
    # Pre-mutate: t338 - differ = True - solver = False - old_items = ['difference_tuple', ' t323', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t338 = difference_tuple(t323, t3)
    except (TypeError, AttributeError, ValueError):
        t338 = _get_safe_default(difference_tuple)
    # Pre-mutate: t339 - differ = True - solver = False - old_items = ['size', ' t338'] - old_hints = ['Integer', 'Container']
    try:
        t339 = size(t338)
    except (TypeError, AttributeError, ValueError):
        t339 = _get_safe_default(size)
    # Pre-mutate: t340 - differ = True - solver = True - old_items = ['get_nth_t', ' t338', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t340 = get_nth_t(t338, F0)
    except (TypeError, AttributeError, ValueError):
        t340 = _get_safe_default(get_nth_t)
    # Pre-mutate: t341 - differ = True - solver = False - old_items = ['astuple', ' t339', ' t340'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t341 = astuple(t339, t340)
    except (TypeError, AttributeError, ValueError):
        t341 = _get_safe_default(astuple)
    s.append((341, '2c608aff', 'differ_fc9e44c913711da609a9e25126b4b296', t341))
    # Pre-mutate: t342 - differ = False - solver = True - old_items = ['chain', ' t291', ' corners', ' outbox'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t342 = chain(t291, corners, outbox)
    except (TypeError, AttributeError, ValueError):
        t342 = _get_safe_default(chain)
    # Pre-mutate: t343 - differ = False - solver = True - old_items = ['colorfilter_t', ' t291', ' TWO'] - old_hints = ['Tuple[Tuple[Tuple[int, int, int], ...], ...]', 'Tuple[Tuple[Tuple[int, int, int], ...], ...]', 'Integer']
    try:
        t343 = colorfilter_t(t291, TWO)
    except (TypeError, AttributeError, ValueError):
        t343 = _get_safe_default(colorfilter_t)
    # Pre-mutate: t344 - differ = False - solver = True - old_items = ['t240', ' t292'] - old_hints = None
    try:
        t344 = t240(t292)
    except (TypeError, AttributeError, ValueError):
        t344 = _get_safe_default(t240)
    # Pre-mutate: t345 - differ = False - solver = True - old_items = ['fill', ' t198', ' ONE', ' t293'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    # GPU Batch Pattern: Sample processing (t345-t348)
    t345, t346, t347, t348 = batch_process_samples_gpu(S)
    t349 = dedupe(t347)
    t350 = dedupe(t348)
    t351 = difference_tuple(t350, t349)
    t352 = get_nth_t(t351, F0)
    try:
        t353 = fill(t198, t352, t293)
    except (TypeError, AttributeError, ValueError):
        t353 = _get_safe_default(fill)
    o.append((353, True, '913fb3ed', t353))
    # Pre-mutate: t354 - differ = True - solver = False - old_items = ['t1', ' t353'] - old_hints = None
    try:
        t354 = t1(t353)
    except (TypeError, AttributeError, ValueError):
        t354 = _get_safe_default(t1)
    # Pre-mutate: t355 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t354'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t355 = difference_tuple(t3, t354)
    except (TypeError, AttributeError, ValueError):
        t355 = _get_safe_default(difference_tuple)
    # Pre-mutate: t356 - differ = True - solver = False - old_items = ['size', ' t355'] - old_hints = ['Integer', 'Container']
    try:
        t356 = size(t355)
    except (TypeError, AttributeError, ValueError):
        t356 = _get_safe_default(size)
    # Pre-mutate: t357 - differ = True - solver = True - old_items = ['get_nth_t', ' t355', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t357 = get_nth_t(t355, F0)
    except (TypeError, AttributeError, ValueError):
        t357 = _get_safe_default(get_nth_t)
    # Pre-mutate: t358 - differ = True - solver = False - old_items = ['astuple', ' t356', ' t357'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t358 = astuple(t356, t357)
    except (TypeError, AttributeError, ValueError):
        t358 = _get_safe_default(astuple)
    s.append((358, '913fb3ed', 'differ_c59e6ae069a9205b311cf67a15473d64', t358))
    # Pre-mutate: t359 - differ = True - solver = False - old_items = ['t8', ' t353'] - old_hints = None
    try:
        t359 = t8(t353)
    except (TypeError, AttributeError, ValueError):
        t359 = _get_safe_default(t8)
    # Pre-mutate: t360 - differ = True - solver = False - old_items = ['t11', ' t359'] - old_hints = None
    try:
        t360 = t11(t359)
    except (TypeError, AttributeError, ValueError):
        t360 = _get_safe_default(t11)
    # Pre-mutate: t361 - differ = True - solver = False - old_items = ['difference', ' t360', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t361 = difference(t360, t13)
    except (TypeError, AttributeError, ValueError):
        t361 = _get_safe_default(difference)
    # Pre-mutate: t362 - differ = True - solver = False - old_items = ['size', ' t361'] - old_hints = ['Integer', 'Container']
    try:
        t362 = size(t361)
    except (TypeError, AttributeError, ValueError):
        t362 = _get_safe_default(size)
    # Pre-mutate: t363 - differ = True - solver = True - old_items = ['get_nth_f', ' t361', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t363 = get_nth_f(t361, F0)
    except (TypeError, AttributeError, ValueError):
        t363 = _get_safe_default(get_nth_f)
    # Pre-mutate: t364 - differ = True - solver = False - old_items = ['astuple', ' t362', ' t363'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t364 = astuple(t362, t363)
    except (TypeError, AttributeError, ValueError):
        t364 = _get_safe_default(astuple)
    s.append((364, '913fb3ed', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t364))
    # Pre-mutate: t365 - differ = True - solver = False - old_items = ['difference', ' t13', ' t360'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t365 = difference(t13, t360)
    except (TypeError, AttributeError, ValueError):
        t365 = _get_safe_default(difference)
    # Pre-mutate: t366 - differ = True - solver = False - old_items = ['size', ' t365'] - old_hints = ['Integer', 'Container']
    try:
        t366 = size(t365)
    except (TypeError, AttributeError, ValueError):
        t366 = _get_safe_default(size)
    # Pre-mutate: t367 - differ = True - solver = True - old_items = ['get_nth_f', ' t365', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t367 = get_nth_f(t365, F0)
    except (TypeError, AttributeError, ValueError):
        t367 = _get_safe_default(get_nth_f)
    # Pre-mutate: t368 - differ = True - solver = False - old_items = ['astuple', ' t366', ' t367'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t368 = astuple(t366, t367)
    except (TypeError, AttributeError, ValueError):
        t368 = _get_safe_default(astuple)
    s.append((368, '913fb3ed', 'differ_f4a4346405e974b919a4b5a52f28e40c', t368))
    # Pre-mutate: t369 - differ = True - solver = False - old_items = ['difference_tuple', ' t354', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t369 = difference_tuple(t354, t3)
    except (TypeError, AttributeError, ValueError):
        t369 = _get_safe_default(difference_tuple)
    # Pre-mutate: t370 - differ = True - solver = False - old_items = ['size', ' t369'] - old_hints = ['Integer', 'Container']
    try:
        t370 = size(t369)
    except (TypeError, AttributeError, ValueError):
        t370 = _get_safe_default(size)
    # Pre-mutate: t371 - differ = True - solver = True - old_items = ['get_nth_t', ' t369', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t371 = get_nth_t(t369, F0)
    except (TypeError, AttributeError, ValueError):
        t371 = _get_safe_default(get_nth_t)
    # Pre-mutate: t372 - differ = True - solver = False - old_items = ['astuple', ' t370', ' t371'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t372 = astuple(t370, t371)
    except (TypeError, AttributeError, ValueError):
        t372 = _get_safe_default(astuple)
    s.append((372, '913fb3ed', 'differ_fc9e44c913711da609a9e25126b4b296', t372))
    # Pre-mutate: t373 - differ = False - solver = True - old_items = ['box', ' t119'] - old_hints = ['Indices', 'Patch']
    try:
        t373 = box(t119)
    except (TypeError, AttributeError, ValueError):
        t373 = _get_safe_default(box)
    # Pre-mutate: t374 - differ = False - solver = True - old_items = ['rbind', ' compose', ' initset'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t374 = rbind(compose, initset)
    except (TypeError, AttributeError, ValueError):
        t374 = _get_safe_default(rbind)
    # Pre-mutate: t375 - differ = False - solver = True - old_items = ['compose', ' t296', ' toindices'] - old_hints = ['Callable', 'Callable', 'Callable']
    try:
        t375 = compose(t296, toindices)
    except (TypeError, AttributeError, ValueError):
        t375 = _get_safe_default(compose)
    # Pre-mutate: t376 - differ = False - solver = True - old_items = ['mfilter_f', ' t43', ' t297'] - old_hints = ['FrozenSet', 'FrozenSet', 'Callable']
    try:
        t376 = mfilter_f(t43, t297)
    except (TypeError, AttributeError, ValueError):
        t376 = _get_safe_default(mfilter_f)
    # Pre-mutate: t377 - differ = False - solver = True - old_items = ['fill', ' t212', ' FOUR', ' t298'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    # GPU Batch Pattern: Sample processing (t377-t380)
    t377, t378, t379, t380 = batch_process_samples_gpu(S)
    t381 = dedupe(t379)
    t382 = dedupe(t380)
    t383 = difference_tuple(t382, t381)
    t384 = get_nth_t(t383, F1)
    try:
        t385 = fill(t212, t384, t298)
    except (TypeError, AttributeError, ValueError):
        t385 = _get_safe_default(fill)
    o.append((385, True, '3bd67248', t385))
    # Pre-mutate: t386 - differ = True - solver = False - old_items = ['t1', ' t385'] - old_hints = None
    try:
        t386 = t1(t385)
    except (TypeError, AttributeError, ValueError):
        t386 = _get_safe_default(t1)
    # Pre-mutate: t387 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t386'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t387 = difference_tuple(t3, t386)
    except (TypeError, AttributeError, ValueError):
        t387 = _get_safe_default(difference_tuple)
    # Pre-mutate: t388 - differ = True - solver = False - old_items = ['size', ' t387'] - old_hints = ['Integer', 'Container']
    try:
        t388 = size(t387)
    except (TypeError, AttributeError, ValueError):
        t388 = _get_safe_default(size)
    # Pre-mutate: t389 - differ = True - solver = True - old_items = ['get_nth_t', ' t387', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t389 = get_nth_t(t387, F0)
    except (TypeError, AttributeError, ValueError):
        t389 = _get_safe_default(get_nth_t)
    # Pre-mutate: t390 - differ = True - solver = False - old_items = ['astuple', ' t388', ' t389'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t390 = astuple(t388, t389)
    except (TypeError, AttributeError, ValueError):
        t390 = _get_safe_default(astuple)
    s.append((390, '3bd67248', 'differ_c59e6ae069a9205b311cf67a15473d64', t390))
    # Pre-mutate: t391 - differ = True - solver = False - old_items = ['t8', ' t385'] - old_hints = None
    try:
        t391 = t8(t385)
    except (TypeError, AttributeError, ValueError):
        t391 = _get_safe_default(t8)
    # Pre-mutate: t392 - differ = True - solver = False - old_items = ['t11', ' t391'] - old_hints = None
    try:
        t392 = t11(t391)
    except (TypeError, AttributeError, ValueError):
        t392 = _get_safe_default(t11)
    # Pre-mutate: t393 - differ = True - solver = False - old_items = ['difference', ' t392', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t393 = difference(t392, t13)
    except (TypeError, AttributeError, ValueError):
        t393 = _get_safe_default(difference)
    # Pre-mutate: t394 - differ = True - solver = False - old_items = ['size', ' t393'] - old_hints = ['Integer', 'Container']
    try:
        t394 = size(t393)
    except (TypeError, AttributeError, ValueError):
        t394 = _get_safe_default(size)
    # Pre-mutate: t395 - differ = True - solver = True - old_items = ['get_nth_f', ' t393', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t395 = get_nth_f(t393, F0)
    except (TypeError, AttributeError, ValueError):
        t395 = _get_safe_default(get_nth_f)
    # Pre-mutate: t396 - differ = True - solver = False - old_items = ['astuple', ' t394', ' t395'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t396 = astuple(t394, t395)
    except (TypeError, AttributeError, ValueError):
        t396 = _get_safe_default(astuple)
    s.append((396, '3bd67248', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t396))
    # Pre-mutate: t397 - differ = True - solver = False - old_items = ['difference', ' t13', ' t392'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t397 = difference(t13, t392)
    except (TypeError, AttributeError, ValueError):
        t397 = _get_safe_default(difference)
    # Pre-mutate: t398 - differ = True - solver = False - old_items = ['size', ' t397'] - old_hints = ['Integer', 'Container']
    try:
        t398 = size(t397)
    except (TypeError, AttributeError, ValueError):
        t398 = _get_safe_default(size)
    # Pre-mutate: t399 - differ = True - solver = True - old_items = ['get_nth_f', ' t397', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t399 = get_nth_f(t397, F0)
    except (TypeError, AttributeError, ValueError):
        t399 = _get_safe_default(get_nth_f)
    # Pre-mutate: t400 - differ = True - solver = False - old_items = ['astuple', ' t398', ' t399'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t400 = astuple(t398, t399)
    except (TypeError, AttributeError, ValueError):
        t400 = _get_safe_default(astuple)
    s.append((400, '3bd67248', 'differ_f4a4346405e974b919a4b5a52f28e40c', t400))
    # Pre-mutate: t401 - differ = True - solver = False - old_items = ['difference_tuple', ' t386', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t401 = difference_tuple(t386, t3)
    except (TypeError, AttributeError, ValueError):
        t401 = _get_safe_default(difference_tuple)
    # Pre-mutate: t402 - differ = True - solver = False - old_items = ['size', ' t401'] - old_hints = ['Integer', 'Container']
    try:
        t402 = size(t401)
    except (TypeError, AttributeError, ValueError):
        t402 = _get_safe_default(size)
    # Pre-mutate: t403 - differ = True - solver = True - old_items = ['get_nth_t', ' t401', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t403 = get_nth_t(t401, F0)
    except (TypeError, AttributeError, ValueError):
        t403 = _get_safe_default(get_nth_t)
    # Pre-mutate: t404 - differ = True - solver = False - old_items = ['astuple', ' t402', ' t403'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t404 = astuple(t402, t403)
    except (TypeError, AttributeError, ValueError):
        t404 = _get_safe_default(astuple)
    s.append((404, '3bd67248', 'differ_fc9e44c913711da609a9e25126b4b296', t404))
    # Pre-mutate: t405 - differ = False - solver = True - old_items = ['astuple', ' TWO', ' FOUR'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t405 = astuple(TWO, FOUR)
    except (TypeError, AttributeError, ValueError):
        t405 = _get_safe_default(astuple)
    # Pre-mutate: t406 - differ = False - solver = True - old_items = ['interval', ' ZERO', ' t320', ' NEG_ONE'] - old_hints = ['Tuple', 'Integer', 'Integer', 'Integer']
    try:
        t406 = interval(ZERO, t320, NEG_ONE)
    except (TypeError, AttributeError, ValueError):
        t406 = _get_safe_default(interval)
    # Pre-mutate: t407 - differ = False - solver = True - old_items = ['chain', ' positive', ' size', ' t321'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t407 = chain(positive, size, t321)
    except (TypeError, AttributeError, ValueError):
        t407 = _get_safe_default(chain)
    # Pre-mutate: t408 - differ = False - solver = True - old_items = ['fork', ' difference', ' t342', ' outbox'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t408 = fork(difference, t342, outbox)
    except (TypeError, AttributeError, ValueError):
        t408 = _get_safe_default(fork)
    # Pre-mutate: t409 - differ = False - solver = True - old_items = ['colorfilter_t', ' t291', ' EIGHT'] - old_hints = ['Tuple[Tuple[Tuple[int, int, int], ...], ...]', 'Tuple[Tuple[Tuple[int, int, int], ...], ...]', 'Integer']
    try:
        t409 = colorfilter_t(t291, EIGHT)
    except (TypeError, AttributeError, ValueError):
        t409 = _get_safe_default(colorfilter_t)
    # Pre-mutate: t410 - differ = False - solver = True - old_items = ['apply', ' t153', ' t344'] - old_hints = ['Container', 'Callable', 'Container']
    try:
        t410 = apply(t153, t344)
    except (TypeError, AttributeError, ValueError):
        t410 = _get_safe_default(apply)
    # Pre-mutate: t411 - differ = False - solver = True - old_items = ['fill', ' t199', ' t294', ' t373'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t411 = fill(t199, t294, t373)
    except (TypeError, AttributeError, ValueError):
        t411 = _get_safe_default(fill)
    o.append((411, False, 'b548a754', t411))
    # Pre-mutate: t412 - differ = True - solver = False - old_items = ['t1', ' t411'] - old_hints = None
    try:
        t412 = t1(t411)
    except (TypeError, AttributeError, ValueError):
        t412 = _get_safe_default(t1)
    # Pre-mutate: t413 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t412'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t413 = difference_tuple(t3, t412)
    except (TypeError, AttributeError, ValueError):
        t413 = _get_safe_default(difference_tuple)
    # Pre-mutate: t414 - differ = True - solver = False - old_items = ['size', ' t413'] - old_hints = ['Integer', 'Container']
    try:
        t414 = size(t413)
    except (TypeError, AttributeError, ValueError):
        t414 = _get_safe_default(size)
    # Pre-mutate: t415 - differ = True - solver = True - old_items = ['get_nth_t', ' t413', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t415 = get_nth_t(t413, F0)
    except (TypeError, AttributeError, ValueError):
        t415 = _get_safe_default(get_nth_t)
    # Pre-mutate: t416 - differ = True - solver = False - old_items = ['astuple', ' t414', ' t415'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t416 = astuple(t414, t415)
    except (TypeError, AttributeError, ValueError):
        t416 = _get_safe_default(astuple)
    s.append((416, 'b548a754', 'differ_c59e6ae069a9205b311cf67a15473d64', t416))
    # Pre-mutate: t417 - differ = True - solver = False - old_items = ['t8', ' t411'] - old_hints = None
    try:
        t417 = t8(t411)
    except (TypeError, AttributeError, ValueError):
        t417 = _get_safe_default(t8)
    # Pre-mutate: t418 - differ = True - solver = False - old_items = ['t11', ' t417'] - old_hints = None
    try:
        t418 = t11(t417)
    except (TypeError, AttributeError, ValueError):
        t418 = _get_safe_default(t11)
    # Pre-mutate: t419 - differ = True - solver = False - old_items = ['difference', ' t418', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t419 = difference(t418, t13)
    except (TypeError, AttributeError, ValueError):
        t419 = _get_safe_default(difference)
    # Pre-mutate: t420 - differ = True - solver = False - old_items = ['size', ' t419'] - old_hints = ['Integer', 'Container']
    try:
        t420 = size(t419)
    except (TypeError, AttributeError, ValueError):
        t420 = _get_safe_default(size)
    # Pre-mutate: t421 - differ = True - solver = True - old_items = ['get_nth_f', ' t419', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t421 = get_nth_f(t419, F0)
    except (TypeError, AttributeError, ValueError):
        t421 = _get_safe_default(get_nth_f)
    # Pre-mutate: t422 - differ = True - solver = False - old_items = ['astuple', ' t420', ' t421'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t422 = astuple(t420, t421)
    except (TypeError, AttributeError, ValueError):
        t422 = _get_safe_default(astuple)
    s.append((422, 'b548a754', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t422))
    # Pre-mutate: t423 - differ = True - solver = False - old_items = ['difference', ' t13', ' t418'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t423 = difference(t13, t418)
    except (TypeError, AttributeError, ValueError):
        t423 = _get_safe_default(difference)
    # Pre-mutate: t424 - differ = True - solver = False - old_items = ['size', ' t423'] - old_hints = ['Integer', 'Container']
    try:
        t424 = size(t423)
    except (TypeError, AttributeError, ValueError):
        t424 = _get_safe_default(size)
    # Pre-mutate: t425 - differ = True - solver = True - old_items = ['get_nth_f', ' t423', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t425 = get_nth_f(t423, F0)
    except (TypeError, AttributeError, ValueError):
        t425 = _get_safe_default(get_nth_f)
    # Pre-mutate: t426 - differ = True - solver = False - old_items = ['astuple', ' t424', ' t425'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t426 = astuple(t424, t425)
    except (TypeError, AttributeError, ValueError):
        t426 = _get_safe_default(astuple)
    s.append((426, 'b548a754', 'differ_f4a4346405e974b919a4b5a52f28e40c', t426))
    # Pre-mutate: t427 - differ = True - solver = False - old_items = ['difference_tuple', ' t412', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t427 = difference_tuple(t412, t3)
    except (TypeError, AttributeError, ValueError):
        t427 = _get_safe_default(difference_tuple)
    # Pre-mutate: t428 - differ = True - solver = False - old_items = ['size', ' t427'] - old_hints = ['Integer', 'Container']
    try:
        t428 = size(t427)
    except (TypeError, AttributeError, ValueError):
        t428 = _get_safe_default(size)
    # Pre-mutate: t429 - differ = True - solver = True - old_items = ['get_nth_t', ' t427', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t429 = get_nth_t(t427, F0)
    except (TypeError, AttributeError, ValueError):
        t429 = _get_safe_default(get_nth_t)
    # Pre-mutate: t430 - differ = True - solver = False - old_items = ['astuple', ' t428', ' t429'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t430 = astuple(t428, t429)
    except (TypeError, AttributeError, ValueError):
        t430 = _get_safe_default(astuple)
    s.append((430, 'b548a754', 'differ_fc9e44c913711da609a9e25126b4b296', t430))
    # Pre-mutate: t431 - differ = False - solver = True - old_items = ['lbind', ' rbind', ' manhattan'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t431 = lbind(rbind, manhattan)
    except (TypeError, AttributeError, ValueError):
        t431 = _get_safe_default(lbind)
    # Pre-mutate: t432 - differ = False - solver = True - old_items = ['fork', ' difference', ' t201', ' t375'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t432 = fork(difference, t201, t375)
    except (TypeError, AttributeError, ValueError):
        t432 = _get_safe_default(fork)
    # Pre-mutate: t433 - differ = False - solver = True - old_items = ['fill', ' I', ' FIVE', ' t376'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t433 = fill(I, GRAY, t376)
    except (TypeError, AttributeError, ValueError):
        t433 = _get_safe_default(fill)
    # Pre-mutate: t434 - differ = False - solver = True - old_items = ['crop', ' I', ' t405', ' THREE_BY_THREE'] - old_hints = ['Grid', 'Grid', 'IJ', 'IJ']
    try:
        t434 = crop(I, t405, THREE_BY_THREE)
    except (TypeError, AttributeError, ValueError):
        t434 = _get_safe_default(crop)
    # Pre-mutate: t435 - differ = False - solver = True - old_items = ['apply', ' t184', ' t406'] - old_hints = ['Container', 'Callable', 'Container']
    try:
        t435 = apply(t184, t406)
    except (TypeError, AttributeError, ValueError):
        t435 = _get_safe_default(apply)
    # Pre-mutate: t436 - differ = False - solver = True - old_items = ['fork', ' both', ' t236', ' t407'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t436 = fork(both, t236, t407)
    except (TypeError, AttributeError, ValueError):
        t436 = _get_safe_default(fork)
    # Pre-mutate: t437 - differ = False - solver = True - old_items = ['chain', ' t151', ' t238', ' t408'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t437 = chain(t151, t238, t408)
    except (TypeError, AttributeError, ValueError):
        t437 = _get_safe_default(chain)
    # Pre-mutate: t438 - differ = False - solver = True - old_items = ['product', ' t343', ' t409'] - old_hints = ['FrozenSet', 'Container', 'Container']
    try:
        t438 = product(t343, t409)
    except (TypeError, AttributeError, ValueError):
        t438 = _get_safe_default(product)
    # Pre-mutate: t439 - differ = False - solver = True - old_items = ['mapply_t', ' t73', ' t410'] - old_hints = ['Tuple', 'Callable', 'Tuple']
    try:
        t439 = mapply_t(t73, t410)
    except (TypeError, AttributeError, ValueError):
        t439 = _get_safe_default(mapply_t)
    # Pre-mutate: t440 - differ = False - solver = True - old_items = ['chain', ' t374', ' t431', ' initset'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t440 = chain(t374, t431, initset)
    except (TypeError, AttributeError, ValueError):
        t440 = _get_safe_default(chain)
    # Pre-mutate: t441 - differ = False - solver = True - old_items = ['asobject', ' t78'] - old_hints = ['Object', 'Grid']
    try:
        t441 = asobject(t78)
    except (TypeError, AttributeError, ValueError):
        t441 = _get_safe_default(asobject)
    # Pre-mutate: t442 - differ = False - solver = True - old_items = ['astuple', ' t299', ' t434'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t442 = astuple(t299, t434)
    except (TypeError, AttributeError, ValueError):
        t442 = _get_safe_default(astuple)
    # Pre-mutate: t443 - differ = False - solver = True - old_items = ['pair', ' t288', ' t435'] - old_hints = ['TupleTuple', 'Tuple', 'Tuple']
    try:
        t443 = pair(t288, t435)
    except (TypeError, AttributeError, ValueError):
        t443 = _get_safe_default(pair)
    # Pre-mutate: t444 - differ = False - solver = True - old_items = ['mfilter_f', ' t185', ' t436'] - old_hints = ['FrozenSet', 'FrozenSet', 'Callable']
    try:
        t444 = mfilter_f(t185, t436)
    except (TypeError, AttributeError, ValueError):
        t444 = _get_safe_default(mfilter_f)
    # Pre-mutate: t445 - differ = False - solver = True - old_items = ['mfilter_f', ' t114', ' t437'] - old_hints = ['FrozenSet', 'FrozenSet', 'Callable']
    try:
        t445 = mfilter_f(t114, t437)
    except (TypeError, AttributeError, ValueError):
        t445 = _get_safe_default(mfilter_f)
    # Pre-mutate: t446 - differ = False - solver = True - old_items = ['apply', ' t239', ' t438'] - old_hints = ['Container', 'Callable', 'Container']
    try:
        t446 = apply(t239, t438)
    except (TypeError, AttributeError, ValueError):
        t446 = _get_safe_default(apply)
    # Pre-mutate: t447 - differ = False - solver = True - old_items = ['paint', ' I', ' t439'] - old_hints = ['Grid', 'Grid', 'Object']
    try:
        t447 = paint(I, t439)
    except (TypeError, AttributeError, ValueError):
        t447 = _get_safe_default(paint)
    # Pre-mutate: t448 - differ = False - solver = True - old_items = ['chain', ' t175', ' t295', ' t440'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t448 = chain(t175, t295, t440)
    except (TypeError, AttributeError, ValueError):
        t448 = _get_safe_default(chain)
    # Pre-mutate: t449 - differ = False - solver = True - old_items = ['mapply_t', ' t432', ' t440'] - old_hints = ['Tuple', 'Callable', 'Tuple']
    try:
        t449 = mapply_t(t432, t440)
    except (TypeError, AttributeError, ValueError):
        t449 = _get_safe_default(mapply_t)
    # Pre-mutate: t450 - differ = False - solver = True - old_items = ['paint', ' t433', ' t441'] - old_hints = ['Grid', 'Grid', 'Object']
    try:
        t450 = paint(t433, t441)
    except (TypeError, AttributeError, ValueError):
        t450 = _get_safe_default(paint)
    o.append((450, False, '63613498', t450))
    # Pre-mutate: t451 - differ = True - solver = False - old_items = ['t1', ' t450'] - old_hints = None
    try:
        t451 = t1(t450)
    except (TypeError, AttributeError, ValueError):
        t451 = _get_safe_default(t1)
    # Pre-mutate: t452 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t451'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t452 = difference_tuple(t3, t451)
    except (TypeError, AttributeError, ValueError):
        t452 = _get_safe_default(difference_tuple)
    # Pre-mutate: t453 - differ = True - solver = False - old_items = ['size', ' t452'] - old_hints = ['Integer', 'Container']
    try:
        t453 = size(t452)
    except (TypeError, AttributeError, ValueError):
        t453 = _get_safe_default(size)
    # Pre-mutate: t454 - differ = True - solver = True - old_items = ['get_nth_t', ' t452', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t454 = get_nth_t(t452, F0)
    except (TypeError, AttributeError, ValueError):
        t454 = _get_safe_default(get_nth_t)
    # Pre-mutate: t455 - differ = True - solver = False - old_items = ['astuple', ' t453', ' t454'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t455 = astuple(t453, t454)
    except (TypeError, AttributeError, ValueError):
        t455 = _get_safe_default(astuple)
    s.append((455, '63613498', 'differ_c59e6ae069a9205b311cf67a15473d64', t455))
    # Pre-mutate: t456 - differ = True - solver = False - old_items = ['t8', ' t450'] - old_hints = None
    try:
        t456 = t8(t450)
    except (TypeError, AttributeError, ValueError):
        t456 = _get_safe_default(t8)
    # Pre-mutate: t457 - differ = True - solver = False - old_items = ['t11', ' t456'] - old_hints = None
    try:
        t457 = t11(t456)
    except (TypeError, AttributeError, ValueError):
        t457 = _get_safe_default(t11)
    # Pre-mutate: t458 - differ = True - solver = False - old_items = ['difference', ' t457', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t458 = difference(t457, t13)
    except (TypeError, AttributeError, ValueError):
        t458 = _get_safe_default(difference)
    # Pre-mutate: t459 - differ = True - solver = False - old_items = ['size', ' t458'] - old_hints = ['Integer', 'Container']
    try:
        t459 = size(t458)
    except (TypeError, AttributeError, ValueError):
        t459 = _get_safe_default(size)
    # Pre-mutate: t460 - differ = True - solver = True - old_items = ['get_nth_f', ' t458', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t460 = get_nth_f(t458, F0)
    except (TypeError, AttributeError, ValueError):
        t460 = _get_safe_default(get_nth_f)
    # Pre-mutate: t461 - differ = True - solver = False - old_items = ['astuple', ' t459', ' t460'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t461 = astuple(t459, t460)
    except (TypeError, AttributeError, ValueError):
        t461 = _get_safe_default(astuple)
    s.append((461, '63613498', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t461))
    # Pre-mutate: t462 - differ = True - solver = False - old_items = ['difference', ' t13', ' t457'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t462 = difference(t13, t457)
    except (TypeError, AttributeError, ValueError):
        t462 = _get_safe_default(difference)
    # Pre-mutate: t463 - differ = True - solver = False - old_items = ['size', ' t462'] - old_hints = ['Integer', 'Container']
    try:
        t463 = size(t462)
    except (TypeError, AttributeError, ValueError):
        t463 = _get_safe_default(size)
    # Pre-mutate: t464 - differ = True - solver = True - old_items = ['get_nth_f', ' t462', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t464 = get_nth_f(t462, F0)
    except (TypeError, AttributeError, ValueError):
        t464 = _get_safe_default(get_nth_f)
    # Pre-mutate: t465 - differ = True - solver = False - old_items = ['astuple', ' t463', ' t464'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t465 = astuple(t463, t464)
    except (TypeError, AttributeError, ValueError):
        t465 = _get_safe_default(astuple)
    s.append((465, '63613498', 'differ_f4a4346405e974b919a4b5a52f28e40c', t465))
    # Pre-mutate: t466 - differ = True - solver = False - old_items = ['difference_tuple', ' t451', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t466 = difference_tuple(t451, t3)
    except (TypeError, AttributeError, ValueError):
        t466 = _get_safe_default(difference_tuple)
    # Pre-mutate: t467 - differ = True - solver = False - old_items = ['size', ' t466'] - old_hints = ['Integer', 'Container']
    try:
        t467 = size(t466)
    except (TypeError, AttributeError, ValueError):
        t467 = _get_safe_default(size)
    # Pre-mutate: t468 - differ = True - solver = True - old_items = ['get_nth_t', ' t466', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t468 = get_nth_t(t466, F0)
    except (TypeError, AttributeError, ValueError):
        t468 = _get_safe_default(get_nth_t)
    # Pre-mutate: t469 - differ = True - solver = False - old_items = ['astuple', ' t467', ' t468'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t469 = astuple(t467, t468)
    except (TypeError, AttributeError, ValueError):
        t469 = _get_safe_default(astuple)
    s.append((469, '63613498', 'differ_fc9e44c913711da609a9e25126b4b296', t469))
    # Pre-mutate: t470 - differ = False - solver = True - old_items = ['combine_t', ' t234', ' t442'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t470 = combine_t(t234, t442)
    except (TypeError, AttributeError, ValueError):
        t470 = _get_safe_default(combine_t)
    # Pre-mutate: t471 - differ = False - solver = True - old_items = ['mapply_t', ' box', ' t443'] - old_hints = ['Tuple', 'Callable', 'Tuple']
    try:
        t471 = mapply_t(box, t443)
    except (TypeError, AttributeError, ValueError):
        t471 = _get_safe_default(mapply_t)
    # Pre-mutate: t472 - differ = False - solver = True - old_items = ['fill', ' t69', ' t112', ' t444'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t472 = fill(t69, t112, t444)
    except (TypeError, AttributeError, ValueError):
        t472 = _get_safe_default(fill)
    o.append((472, False, '6cdd2623', t472))
    # Pre-mutate: t473 - differ = True - solver = False - old_items = ['t1', ' t472'] - old_hints = None
    try:
        t473 = t1(t472)
    except (TypeError, AttributeError, ValueError):
        t473 = _get_safe_default(t1)
    # Pre-mutate: t474 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t473'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t474 = difference_tuple(t3, t473)
    except (TypeError, AttributeError, ValueError):
        t474 = _get_safe_default(difference_tuple)
    # Pre-mutate: t475 - differ = True - solver = False - old_items = ['size', ' t474'] - old_hints = ['Integer', 'Container']
    try:
        t475 = size(t474)
    except (TypeError, AttributeError, ValueError):
        t475 = _get_safe_default(size)
    # Pre-mutate: t476 - differ = True - solver = True - old_items = ['get_nth_t', ' t474', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t476 = get_nth_t(t474, F0)
    except (TypeError, AttributeError, ValueError):
        t476 = _get_safe_default(get_nth_t)
    # Pre-mutate: t477 - differ = True - solver = False - old_items = ['astuple', ' t475', ' t476'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t477 = astuple(t475, t476)
    except (TypeError, AttributeError, ValueError):
        t477 = _get_safe_default(astuple)
    s.append((477, '6cdd2623', 'differ_c59e6ae069a9205b311cf67a15473d64', t477))
    # Pre-mutate: t478 - differ = True - solver = False - old_items = ['t8', ' t472'] - old_hints = None
    try:
        t478 = t8(t472)
    except (TypeError, AttributeError, ValueError):
        t478 = _get_safe_default(t8)
    # Pre-mutate: t479 - differ = True - solver = False - old_items = ['t11', ' t478'] - old_hints = None
    try:
        t479 = t11(t478)
    except (TypeError, AttributeError, ValueError):
        t479 = _get_safe_default(t11)
    # Pre-mutate: t480 - differ = True - solver = False - old_items = ['difference', ' t479', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t480 = difference(t479, t13)
    except (TypeError, AttributeError, ValueError):
        t480 = _get_safe_default(difference)
    # Pre-mutate: t481 - differ = True - solver = False - old_items = ['size', ' t480'] - old_hints = ['Integer', 'Container']
    try:
        t481 = size(t480)
    except (TypeError, AttributeError, ValueError):
        t481 = _get_safe_default(size)
    # Pre-mutate: t482 - differ = True - solver = True - old_items = ['get_nth_f', ' t480', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t482 = get_nth_f(t480, F0)
    except (TypeError, AttributeError, ValueError):
        t482 = _get_safe_default(get_nth_f)
    # Pre-mutate: t483 - differ = True - solver = False - old_items = ['astuple', ' t481', ' t482'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t483 = astuple(t481, t482)
    except (TypeError, AttributeError, ValueError):
        t483 = _get_safe_default(astuple)
    s.append((483, '6cdd2623', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t483))
    # Pre-mutate: t484 - differ = True - solver = False - old_items = ['difference', ' t13', ' t479'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t484 = difference(t13, t479)
    except (TypeError, AttributeError, ValueError):
        t484 = _get_safe_default(difference)
    # Pre-mutate: t485 - differ = True - solver = False - old_items = ['size', ' t484'] - old_hints = ['Integer', 'Container']
    try:
        t485 = size(t484)
    except (TypeError, AttributeError, ValueError):
        t485 = _get_safe_default(size)
    # Pre-mutate: t486 - differ = True - solver = True - old_items = ['get_nth_f', ' t484', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t486 = get_nth_f(t484, F0)
    except (TypeError, AttributeError, ValueError):
        t486 = _get_safe_default(get_nth_f)
    # Pre-mutate: t487 - differ = True - solver = False - old_items = ['astuple', ' t485', ' t486'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t487 = astuple(t485, t486)
    except (TypeError, AttributeError, ValueError):
        t487 = _get_safe_default(astuple)
    s.append((487, '6cdd2623', 'differ_f4a4346405e974b919a4b5a52f28e40c', t487))
    # Pre-mutate: t488 - differ = True - solver = False - old_items = ['difference_tuple', ' t473', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t488 = difference_tuple(t473, t3)
    except (TypeError, AttributeError, ValueError):
        t488 = _get_safe_default(difference_tuple)
    # Pre-mutate: t489 - differ = True - solver = False - old_items = ['size', ' t488'] - old_hints = ['Integer', 'Container']
    try:
        t489 = size(t488)
    except (TypeError, AttributeError, ValueError):
        t489 = _get_safe_default(size)
    # Pre-mutate: t490 - differ = True - solver = True - old_items = ['get_nth_t', ' t488', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t490 = get_nth_t(t488, F0)
    except (TypeError, AttributeError, ValueError):
        t490 = _get_safe_default(get_nth_t)
    # Pre-mutate: t491 - differ = True - solver = False - old_items = ['astuple', ' t489', ' t490'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t491 = astuple(t489, t490)
    except (TypeError, AttributeError, ValueError):
        t491 = _get_safe_default(astuple)
    s.append((491, '6cdd2623', 'differ_fc9e44c913711da609a9e25126b4b296', t491))
    # Pre-mutate: t492 - differ = False - solver = True - old_items = ['fill', ' I', ' FOUR', ' t445'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t492 = fill(I, YELLOW, t445)
    except (TypeError, AttributeError, ValueError):
        t492 = _get_safe_default(fill)
    o.append((492, True, 'e73095fd', t492))
    # Pre-mutate: t493 - differ = True - solver = False - old_items = ['t1', ' t492'] - old_hints = None
    try:
        t493 = t1(t492)
    except (TypeError, AttributeError, ValueError):
        t493 = _get_safe_default(t1)
    # Pre-mutate: t494 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t493'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t494 = difference_tuple(t3, t493)
    except (TypeError, AttributeError, ValueError):
        t494 = _get_safe_default(difference_tuple)
    # Pre-mutate: t495 - differ = True - solver = False - old_items = ['size', ' t494'] - old_hints = ['Integer', 'Container']
    try:
        t495 = size(t494)
    except (TypeError, AttributeError, ValueError):
        t495 = _get_safe_default(size)
    # Pre-mutate: t496 - differ = True - solver = True - old_items = ['get_nth_t', ' t494', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t496 = get_nth_t(t494, F0)
    except (TypeError, AttributeError, ValueError):
        t496 = _get_safe_default(get_nth_t)
    # Pre-mutate: t497 - differ = True - solver = False - old_items = ['astuple', ' t495', ' t496'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t497 = astuple(t495, t496)
    except (TypeError, AttributeError, ValueError):
        t497 = _get_safe_default(astuple)
    s.append((497, 'e73095fd', 'differ_c59e6ae069a9205b311cf67a15473d64', t497))
    # Pre-mutate: t498 - differ = True - solver = False - old_items = ['t8', ' t492'] - old_hints = None
    try:
        t498 = t8(t492)
    except (TypeError, AttributeError, ValueError):
        t498 = _get_safe_default(t8)
    # Pre-mutate: t499 - differ = True - solver = False - old_items = ['t11', ' t498'] - old_hints = None
    try:
        t499 = t11(t498)
    except (TypeError, AttributeError, ValueError):
        t499 = _get_safe_default(t11)
    # Pre-mutate: t500 - differ = True - solver = False - old_items = ['difference', ' t499', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t500 = difference(t499, t13)
    except (TypeError, AttributeError, ValueError):
        t500 = _get_safe_default(difference)
    # Pre-mutate: t501 - differ = True - solver = False - old_items = ['size', ' t500'] - old_hints = ['Integer', 'Container']
    try:
        t501 = size(t500)
    except (TypeError, AttributeError, ValueError):
        t501 = _get_safe_default(size)
    # Pre-mutate: t502 - differ = True - solver = True - old_items = ['get_nth_f', ' t500', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t502 = get_nth_f(t500, F0)
    except (TypeError, AttributeError, ValueError):
        t502 = _get_safe_default(get_nth_f)
    # Pre-mutate: t503 - differ = True - solver = False - old_items = ['astuple', ' t501', ' t502'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t503 = astuple(t501, t502)
    except (TypeError, AttributeError, ValueError):
        t503 = _get_safe_default(astuple)
    s.append((503, 'e73095fd', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t503))
    # Pre-mutate: t504 - differ = True - solver = False - old_items = ['difference', ' t13', ' t499'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t504 = difference(t13, t499)
    except (TypeError, AttributeError, ValueError):
        t504 = _get_safe_default(difference)
    # Pre-mutate: t505 - differ = True - solver = False - old_items = ['size', ' t504'] - old_hints = ['Integer', 'Container']
    try:
        t505 = size(t504)
    except (TypeError, AttributeError, ValueError):
        t505 = _get_safe_default(size)
    # Pre-mutate: t506 - differ = True - solver = True - old_items = ['get_nth_f', ' t504', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t506 = get_nth_f(t504, F0)
    except (TypeError, AttributeError, ValueError):
        t506 = _get_safe_default(get_nth_f)
    # Pre-mutate: t507 - differ = True - solver = False - old_items = ['astuple', ' t505', ' t506'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t507 = astuple(t505, t506)
    except (TypeError, AttributeError, ValueError):
        t507 = _get_safe_default(astuple)
    s.append((507, 'e73095fd', 'differ_f4a4346405e974b919a4b5a52f28e40c', t507))
    # Pre-mutate: t508 - differ = True - solver = False - old_items = ['difference_tuple', ' t493', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t508 = difference_tuple(t493, t3)
    except (TypeError, AttributeError, ValueError):
        t508 = _get_safe_default(difference_tuple)
    # Pre-mutate: t509 - differ = True - solver = False - old_items = ['size', ' t508'] - old_hints = ['Integer', 'Container']
    try:
        t509 = size(t508)
    except (TypeError, AttributeError, ValueError):
        t509 = _get_safe_default(size)
    # Pre-mutate: t510 - differ = True - solver = True - old_items = ['get_nth_t', ' t508', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t510 = get_nth_t(t508, F0)
    except (TypeError, AttributeError, ValueError):
        t510 = _get_safe_default(get_nth_t)
    # Pre-mutate: t511 - differ = True - solver = False - old_items = ['astuple', ' t509', ' t510'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t511 = astuple(t509, t510)
    except (TypeError, AttributeError, ValueError):
        t511 = _get_safe_default(astuple)
    s.append((511, 'e73095fd', 'differ_fc9e44c913711da609a9e25126b4b296', t511))
    # Pre-mutate: t512 - differ = False - solver = True - old_items = ['lbind', ' greater', ' EIGHT'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t512 = lbind(greater, EIGHT)
    except (TypeError, AttributeError, ValueError):
        t512 = _get_safe_default(lbind)
    # Pre-mutate: t513 - differ = False - solver = True - old_items = ['asindices', ' I'] - old_hints = ['Indices', 'Grid']
    try:
        t513 = asindices(I)
    except (TypeError, AttributeError, ValueError):
        t513 = _get_safe_default(asindices)
    # Pre-mutate: t514 - differ = False - solver = True - old_items = ['fork', ' ascell', ' identity', ' t448'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t514 = fork(ascell, identity, t448)
    except (TypeError, AttributeError, ValueError):
        t514 = _get_safe_default(fork)
    # Pre-mutate: t515 - differ = False - solver = True - old_items = ['fill', ' I', ' EIGHT', ' t449'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    # GPU Batch Pattern: Sample processing (t515-t518)
    t515, t516, t517, t518 = batch_process_samples_gpu(S)
    t519 = dedupe(t517)
    t520 = dedupe(t518)
    t521 = difference_tuple(t520, t519)
    t522 = get_nth_t(t521, F0)
    try:
        t523 = fill(I, t522, t449)
    except (TypeError, AttributeError, ValueError):
        t523 = _get_safe_default(fill)
    o.append((523, True, '22233c11', t523))
    # Pre-mutate: t524 - differ = True - solver = False - old_items = ['t1', ' t523'] - old_hints = None
    try:
        t524 = t1(t523)
    except (TypeError, AttributeError, ValueError):
        t524 = _get_safe_default(t1)
    # Pre-mutate: t525 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t524'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t525 = difference_tuple(t3, t524)
    except (TypeError, AttributeError, ValueError):
        t525 = _get_safe_default(difference_tuple)
    # Pre-mutate: t526 - differ = True - solver = False - old_items = ['size', ' t525'] - old_hints = ['Integer', 'Container']
    try:
        t526 = size(t525)
    except (TypeError, AttributeError, ValueError):
        t526 = _get_safe_default(size)
    # Pre-mutate: t527 - differ = True - solver = True - old_items = ['get_nth_t', ' t525', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t527 = get_nth_t(t525, F0)
    except (TypeError, AttributeError, ValueError):
        t527 = _get_safe_default(get_nth_t)
    # Pre-mutate: t528 - differ = True - solver = False - old_items = ['astuple', ' t526', ' t527'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t528 = astuple(t526, t527)
    except (TypeError, AttributeError, ValueError):
        t528 = _get_safe_default(astuple)
    s.append((528, '22233c11', 'differ_c59e6ae069a9205b311cf67a15473d64', t528))
    # Pre-mutate: t529 - differ = True - solver = False - old_items = ['t8', ' t523'] - old_hints = None
    try:
        t529 = t8(t523)
    except (TypeError, AttributeError, ValueError):
        t529 = _get_safe_default(t8)
    # Pre-mutate: t530 - differ = True - solver = False - old_items = ['t11', ' t529'] - old_hints = None
    try:
        t530 = t11(t529)
    except (TypeError, AttributeError, ValueError):
        t530 = _get_safe_default(t11)
    # Pre-mutate: t531 - differ = True - solver = False - old_items = ['difference', ' t530', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t531 = difference(t530, t13)
    except (TypeError, AttributeError, ValueError):
        t531 = _get_safe_default(difference)
    # Pre-mutate: t532 - differ = True - solver = False - old_items = ['size', ' t531'] - old_hints = ['Integer', 'Container']
    try:
        t532 = size(t531)
    except (TypeError, AttributeError, ValueError):
        t532 = _get_safe_default(size)
    # Pre-mutate: t533 - differ = True - solver = True - old_items = ['get_nth_f', ' t531', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t533 = get_nth_f(t531, F0)
    except (TypeError, AttributeError, ValueError):
        t533 = _get_safe_default(get_nth_f)
    # Pre-mutate: t534 - differ = True - solver = False - old_items = ['astuple', ' t532', ' t533'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t534 = astuple(t532, t533)
    except (TypeError, AttributeError, ValueError):
        t534 = _get_safe_default(astuple)
    s.append((534, '22233c11', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t534))
    # Pre-mutate: t535 - differ = True - solver = False - old_items = ['difference', ' t13', ' t530'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t535 = difference(t13, t530)
    except (TypeError, AttributeError, ValueError):
        t535 = _get_safe_default(difference)
    # Pre-mutate: t536 - differ = True - solver = False - old_items = ['size', ' t535'] - old_hints = ['Integer', 'Container']
    try:
        t536 = size(t535)
    except (TypeError, AttributeError, ValueError):
        t536 = _get_safe_default(size)
    # Pre-mutate: t537 - differ = True - solver = True - old_items = ['get_nth_f', ' t535', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t537 = get_nth_f(t535, F0)
    except (TypeError, AttributeError, ValueError):
        t537 = _get_safe_default(get_nth_f)
    # Pre-mutate: t538 - differ = True - solver = False - old_items = ['astuple', ' t536', ' t537'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t538 = astuple(t536, t537)
    except (TypeError, AttributeError, ValueError):
        t538 = _get_safe_default(astuple)
    s.append((538, '22233c11', 'differ_f4a4346405e974b919a4b5a52f28e40c', t538))
    # Pre-mutate: t539 - differ = True - solver = False - old_items = ['difference_tuple', ' t524', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t539 = difference_tuple(t524, t3)
    except (TypeError, AttributeError, ValueError):
        t539 = _get_safe_default(difference_tuple)
    # Pre-mutate: t540 - differ = True - solver = False - old_items = ['size', ' t539'] - old_hints = ['Integer', 'Container']
    try:
        t540 = size(t539)
    except (TypeError, AttributeError, ValueError):
        t540 = _get_safe_default(size)
    # Pre-mutate: t541 - differ = True - solver = True - old_items = ['get_nth_t', ' t539', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t541 = get_nth_t(t539, F0)
    except (TypeError, AttributeError, ValueError):
        t541 = _get_safe_default(get_nth_t)
    # Pre-mutate: t542 - differ = True - solver = False - old_items = ['astuple', ' t540', ' t541'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t542 = astuple(t540, t541)
    except (TypeError, AttributeError, ValueError):
        t542 = _get_safe_default(astuple)
    s.append((542, '22233c11', 'differ_fc9e44c913711da609a9e25126b4b296', t542))
    # Pre-mutate: t543 - differ = False - solver = True - old_items = ['mapply_t', ' t109', ' t470'] - old_hints = ['Tuple', 'Callable', 'Tuple']
    try:
        t543 = mapply_t(t109, t470)
    except (TypeError, AttributeError, ValueError):
        t543 = _get_safe_default(mapply_t)
    # Pre-mutate: t544 - differ = False - solver = True - old_items = ['shift', ' t471', ' t68'] - old_hints = ['Patch', 'Patch', 'IJ']
    try:
        t544 = shift(t471, t68)
    except (TypeError, AttributeError, ValueError):
        t544 = _get_safe_default(shift)
    # Pre-mutate: t545 - differ = False - solver = True - old_items = ['compose', ' t512', ' size'] - old_hints = ['Callable', 'Callable', 'Callable']
    try:
        t545 = compose(t512, size)
    except (TypeError, AttributeError, ValueError):
        t545 = _get_safe_default(compose)
    # Pre-mutate: t546 - differ = False - solver = True - old_items = ['normalize_o', ' t58'] - old_hints = ['Object', 'Object']
    try:
        t546 = normalize_o(t58)
    except (TypeError, AttributeError, ValueError):
        t546 = _get_safe_default(normalize_o)
    # Pre-mutate: t547 - differ = False - solver = True - old_items = ['fill', ' t47', ' t65', ' t543'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t547 = fill(t47, t65, t543)
    except (TypeError, AttributeError, ValueError):
        t547 = _get_safe_default(fill)
    o.append((547, False, 'bc1d5164', t547))
    # Pre-mutate: t548 - differ = True - solver = False - old_items = ['t1', ' t547'] - old_hints = None
    try:
        t548 = t1(t547)
    except (TypeError, AttributeError, ValueError):
        t548 = _get_safe_default(t1)
    # Pre-mutate: t549 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t548'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t549 = difference_tuple(t3, t548)
    except (TypeError, AttributeError, ValueError):
        t549 = _get_safe_default(difference_tuple)
    # Pre-mutate: t550 - differ = True - solver = False - old_items = ['size', ' t549'] - old_hints = ['Integer', 'Container']
    try:
        t550 = size(t549)
    except (TypeError, AttributeError, ValueError):
        t550 = _get_safe_default(size)
    # Pre-mutate: t551 - differ = True - solver = True - old_items = ['get_nth_t', ' t549', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t551 = get_nth_t(t549, F0)
    except (TypeError, AttributeError, ValueError):
        t551 = _get_safe_default(get_nth_t)
    # Pre-mutate: t552 - differ = True - solver = False - old_items = ['astuple', ' t550', ' t551'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t552 = astuple(t550, t551)
    except (TypeError, AttributeError, ValueError):
        t552 = _get_safe_default(astuple)
    s.append((552, 'bc1d5164', 'differ_c59e6ae069a9205b311cf67a15473d64', t552))
    # Pre-mutate: t553 - differ = True - solver = False - old_items = ['t8', ' t547'] - old_hints = None
    try:
        t553 = t8(t547)
    except (TypeError, AttributeError, ValueError):
        t553 = _get_safe_default(t8)
    # Pre-mutate: t554 - differ = True - solver = False - old_items = ['t11', ' t553'] - old_hints = None
    try:
        t554 = t11(t553)
    except (TypeError, AttributeError, ValueError):
        t554 = _get_safe_default(t11)
    # Pre-mutate: t555 - differ = True - solver = False - old_items = ['difference', ' t554', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t555 = difference(t554, t13)
    except (TypeError, AttributeError, ValueError):
        t555 = _get_safe_default(difference)
    # Pre-mutate: t556 - differ = True - solver = False - old_items = ['size', ' t555'] - old_hints = ['Integer', 'Container']
    try:
        t556 = size(t555)
    except (TypeError, AttributeError, ValueError):
        t556 = _get_safe_default(size)
    # Pre-mutate: t557 - differ = True - solver = True - old_items = ['get_nth_f', ' t555', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t557 = get_nth_f(t555, F0)
    except (TypeError, AttributeError, ValueError):
        t557 = _get_safe_default(get_nth_f)
    # Pre-mutate: t558 - differ = True - solver = False - old_items = ['astuple', ' t556', ' t557'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t558 = astuple(t556, t557)
    except (TypeError, AttributeError, ValueError):
        t558 = _get_safe_default(astuple)
    s.append((558, 'bc1d5164', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t558))
    # Pre-mutate: t559 - differ = True - solver = False - old_items = ['difference', ' t13', ' t554'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t559 = difference(t13, t554)
    except (TypeError, AttributeError, ValueError):
        t559 = _get_safe_default(difference)
    # Pre-mutate: t560 - differ = True - solver = False - old_items = ['size', ' t559'] - old_hints = ['Integer', 'Container']
    try:
        t560 = size(t559)
    except (TypeError, AttributeError, ValueError):
        t560 = _get_safe_default(size)
    # Pre-mutate: t561 - differ = True - solver = True - old_items = ['get_nth_f', ' t559', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t561 = get_nth_f(t559, F0)
    except (TypeError, AttributeError, ValueError):
        t561 = _get_safe_default(get_nth_f)
    # Pre-mutate: t562 - differ = True - solver = False - old_items = ['astuple', ' t560', ' t561'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t562 = astuple(t560, t561)
    except (TypeError, AttributeError, ValueError):
        t562 = _get_safe_default(astuple)
    s.append((562, 'bc1d5164', 'differ_f4a4346405e974b919a4b5a52f28e40c', t562))
    # Pre-mutate: t563 - differ = True - solver = False - old_items = ['difference_tuple', ' t548', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t563 = difference_tuple(t548, t3)
    except (TypeError, AttributeError, ValueError):
        t563 = _get_safe_default(difference_tuple)
    # Pre-mutate: t564 - differ = True - solver = False - old_items = ['size', ' t563'] - old_hints = ['Integer', 'Container']
    try:
        t564 = size(t563)
    except (TypeError, AttributeError, ValueError):
        t564 = _get_safe_default(size)
    # Pre-mutate: t565 - differ = True - solver = True - old_items = ['get_nth_t', ' t563', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t565 = get_nth_t(t563, F0)
    except (TypeError, AttributeError, ValueError):
        t565 = _get_safe_default(get_nth_t)
    # Pre-mutate: t566 - differ = True - solver = False - old_items = ['astuple', ' t564', ' t565'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t566 = astuple(t564, t565)
    except (TypeError, AttributeError, ValueError):
        t566 = _get_safe_default(astuple)
    s.append((566, 'bc1d5164', 'differ_fc9e44c913711da609a9e25126b4b296', t566))
    # Pre-mutate: t567 - differ = False - solver = True - old_items = ['fill', ' I', ' t26', ' t544'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t567 = fill(I, t26, t544)
    except (TypeError, AttributeError, ValueError):
        t567 = _get_safe_default(fill)
    o.append((567, False, '5c2c9af4', t567))
    # Pre-mutate: t568 - differ = True - solver = False - old_items = ['t1', ' t567'] - old_hints = None
    try:
        t568 = t1(t567)
    except (TypeError, AttributeError, ValueError):
        t568 = _get_safe_default(t1)
    # Pre-mutate: t569 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t568'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t569 = difference_tuple(t3, t568)
    except (TypeError, AttributeError, ValueError):
        t569 = _get_safe_default(difference_tuple)
    # Pre-mutate: t570 - differ = True - solver = False - old_items = ['size', ' t569'] - old_hints = ['Integer', 'Container']
    try:
        t570 = size(t569)
    except (TypeError, AttributeError, ValueError):
        t570 = _get_safe_default(size)
    # Pre-mutate: t571 - differ = True - solver = True - old_items = ['get_nth_t', ' t569', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t571 = get_nth_t(t569, F0)
    except (TypeError, AttributeError, ValueError):
        t571 = _get_safe_default(get_nth_t)
    # Pre-mutate: t572 - differ = True - solver = False - old_items = ['astuple', ' t570', ' t571'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t572 = astuple(t570, t571)
    except (TypeError, AttributeError, ValueError):
        t572 = _get_safe_default(astuple)
    s.append((572, '5c2c9af4', 'differ_c59e6ae069a9205b311cf67a15473d64', t572))
    # Pre-mutate: t573 - differ = True - solver = False - old_items = ['t8', ' t567'] - old_hints = None
    try:
        t573 = t8(t567)
    except (TypeError, AttributeError, ValueError):
        t573 = _get_safe_default(t8)
    # Pre-mutate: t574 - differ = True - solver = False - old_items = ['t11', ' t573'] - old_hints = None
    try:
        t574 = t11(t573)
    except (TypeError, AttributeError, ValueError):
        t574 = _get_safe_default(t11)
    # Pre-mutate: t575 - differ = True - solver = False - old_items = ['difference', ' t574', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t575 = difference(t574, t13)
    except (TypeError, AttributeError, ValueError):
        t575 = _get_safe_default(difference)
    # Pre-mutate: t576 - differ = True - solver = False - old_items = ['size', ' t575'] - old_hints = ['Integer', 'Container']
    try:
        t576 = size(t575)
    except (TypeError, AttributeError, ValueError):
        t576 = _get_safe_default(size)
    # Pre-mutate: t577 - differ = True - solver = True - old_items = ['get_nth_f', ' t575', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t577 = get_nth_f(t575, F0)
    except (TypeError, AttributeError, ValueError):
        t577 = _get_safe_default(get_nth_f)
    # Pre-mutate: t578 - differ = True - solver = False - old_items = ['astuple', ' t576', ' t577'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t578 = astuple(t576, t577)
    except (TypeError, AttributeError, ValueError):
        t578 = _get_safe_default(astuple)
    s.append((578, '5c2c9af4', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t578))
    # Pre-mutate: t579 - differ = True - solver = False - old_items = ['difference', ' t13', ' t574'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t579 = difference(t13, t574)
    except (TypeError, AttributeError, ValueError):
        t579 = _get_safe_default(difference)
    # Pre-mutate: t580 - differ = True - solver = False - old_items = ['size', ' t579'] - old_hints = ['Integer', 'Container']
    try:
        t580 = size(t579)
    except (TypeError, AttributeError, ValueError):
        t580 = _get_safe_default(size)
    # Pre-mutate: t581 - differ = True - solver = True - old_items = ['get_nth_f', ' t579', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t581 = get_nth_f(t579, F0)
    except (TypeError, AttributeError, ValueError):
        t581 = _get_safe_default(get_nth_f)
    # Pre-mutate: t582 - differ = True - solver = False - old_items = ['astuple', ' t580', ' t581'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t582 = astuple(t580, t581)
    except (TypeError, AttributeError, ValueError):
        t582 = _get_safe_default(astuple)
    s.append((582, '5c2c9af4', 'differ_f4a4346405e974b919a4b5a52f28e40c', t582))
    # Pre-mutate: t583 - differ = True - solver = False - old_items = ['difference_tuple', ' t568', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t583 = difference_tuple(t568, t3)
    except (TypeError, AttributeError, ValueError):
        t583 = _get_safe_default(difference_tuple)
    # Pre-mutate: t584 - differ = True - solver = False - old_items = ['size', ' t583'] - old_hints = ['Integer', 'Container']
    try:
        t584 = size(t583)
    except (TypeError, AttributeError, ValueError):
        t584 = _get_safe_default(size)
    # Pre-mutate: t585 - differ = True - solver = True - old_items = ['get_nth_t', ' t583', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t585 = get_nth_t(t583, F0)
    except (TypeError, AttributeError, ValueError):
        t585 = _get_safe_default(get_nth_t)
    # Pre-mutate: t586 - differ = True - solver = False - old_items = ['astuple', ' t584', ' t585'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t586 = astuple(t584, t585)
    except (TypeError, AttributeError, ValueError):
        t586 = _get_safe_default(astuple)
    s.append((586, '5c2c9af4', 'differ_fc9e44c913711da609a9e25126b4b296', t586))
    # Pre-mutate: t587 - differ = False - solver = True - old_items = ['mfilter_f', ' t446', ' t545'] - old_hints = ['FrozenSet', 'FrozenSet', 'Callable']
    try:
        t587 = mfilter_f(t446, t545)
    except (TypeError, AttributeError, ValueError):
        t587 = _get_safe_default(mfilter_f)
    # Pre-mutate: t588 - differ = False - solver = True - old_items = ['f_ofcolor', ' I', ' t545'] - old_hints = ['Indices', 'Grid', 'C_']
    try:
        t588 = f_ofcolor(I, t545)
    except (TypeError, AttributeError, ValueError):
        t588 = _get_safe_default(f_ofcolor)
    # Pre-mutate: t589 - differ = False - solver = True - old_items = ['shift', ' t546', ' UNITY'] - old_hints = ['Patch', 'Patch', 'IJ']
    try:
        t589 = shift(t546, UNITY)
    except (TypeError, AttributeError, ValueError):
        t589 = _get_safe_default(shift)
    # Pre-mutate: t590 - differ = False - solver = True - old_items = ['fill', ' I', ' TWO', ' t587'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t590 = fill(I, RED, t587)
    except (TypeError, AttributeError, ValueError):
        t590 = _get_safe_default(fill)
    # Pre-mutate: t591 - differ = False - solver = True - old_items = ['lbind', ' hmatching', ' t588'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t591 = lbind(hmatching, t588)
    except (TypeError, AttributeError, ValueError):
        t591 = _get_safe_default(lbind)
    # Pre-mutate: t592 - differ = False - solver = True - old_items = ['toindices', ' t589'] - old_hints = ['Indices', 'Patch']
    try:
        t592 = toindices(t589)
    except (TypeError, AttributeError, ValueError):
        t592 = _get_safe_default(toindices)
    # Pre-mutate: t593 - differ = False - solver = True - old_items = ['apply', ' t188', ' t438'] - old_hints = ['Container', 'Callable', 'Container']
    try:
        t593 = apply(t188, t438)
    except (TypeError, AttributeError, ValueError):
        t593 = _get_safe_default(apply)
    # Pre-mutate: t594 - differ = False - solver = True - old_items = ['lbind', ' vmatching', ' t588'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t594 = lbind(vmatching, t588)
    except (TypeError, AttributeError, ValueError):
        t594 = _get_safe_default(lbind)
    # Pre-mutate: t595 - differ = False - solver = True - old_items = ['apply', ' t514', ' t592'] - old_hints = ['Container', 'Callable', 'Container']
    try:
        t595 = apply(t514, t592)
    except (TypeError, AttributeError, ValueError):
        t595 = _get_safe_default(apply)
    # Pre-mutate: t596 - differ = False - solver = True - old_items = ['intersection', ' t587', ' t593'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t596 = intersection(t587, t593)
    except (TypeError, AttributeError, ValueError):
        t596 = _get_safe_default(intersection)
    # Pre-mutate: t597 - differ = False - solver = True - old_items = ['fork', ' either', ' t591', ' t594'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t597 = fork(either, t591, t594)
    except (TypeError, AttributeError, ValueError):
        t597 = _get_safe_default(fork)
    # Pre-mutate: t598 - differ = False - solver = True - old_items = ['paint', ' t120', ' t595'] - old_hints = ['Grid', 'Grid', 'Object']
    try:
        t598 = paint(t120, t595)
    except (TypeError, AttributeError, ValueError):
        t598 = _get_safe_default(paint)
    # Pre-mutate: t599 - differ = False - solver = True - old_items = ['mapply_t', ' neighbors', ' t596'] - old_hints = ['Tuple', 'Callable', 'Tuple']
    try:
        t599 = mapply_t(neighbors, t596)
    except (TypeError, AttributeError, ValueError):
        t599 = _get_safe_default(mapply_t)
    # Pre-mutate: t600 - differ = False - solver = True - old_items = ['compose', ' t597', ' initset'] - old_hints = ['Callable', 'Callable', 'Callable']
    try:
        t600 = compose(t597, initset)
    except (TypeError, AttributeError, ValueError):
        t600 = _get_safe_default(compose)
    # Pre-mutate: t601 - differ = False - solver = True - old_items = ['fill', ' t590', ' EIGHT', ' t599'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t601 = fill(t590, CYAN, t599)
    except (TypeError, AttributeError, ValueError):
        t601 = _get_safe_default(fill)
    o.append((601, True, 'ecdecbb3', t601))
    # Pre-mutate: t602 - differ = True - solver = False - old_items = ['t1', ' t601'] - old_hints = None
    try:
        t602 = t1(t601)
    except (TypeError, AttributeError, ValueError):
        t602 = _get_safe_default(t1)
    # Pre-mutate: t603 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t602'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t603 = difference_tuple(t3, t602)
    except (TypeError, AttributeError, ValueError):
        t603 = _get_safe_default(difference_tuple)
    # Pre-mutate: t604 - differ = True - solver = False - old_items = ['size', ' t603'] - old_hints = ['Integer', 'Container']
    try:
        t604 = size(t603)
    except (TypeError, AttributeError, ValueError):
        t604 = _get_safe_default(size)
    # Pre-mutate: t605 - differ = True - solver = True - old_items = ['get_nth_t', ' t603', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t605 = get_nth_t(t603, F0)
    except (TypeError, AttributeError, ValueError):
        t605 = _get_safe_default(get_nth_t)
    # Pre-mutate: t606 - differ = True - solver = False - old_items = ['astuple', ' t604', ' t605'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t606 = astuple(t604, t605)
    except (TypeError, AttributeError, ValueError):
        t606 = _get_safe_default(astuple)
    s.append((606, 'ecdecbb3', 'differ_c59e6ae069a9205b311cf67a15473d64', t606))
    # Pre-mutate: t607 - differ = True - solver = False - old_items = ['t8', ' t601'] - old_hints = None
    try:
        t607 = t8(t601)
    except (TypeError, AttributeError, ValueError):
        t607 = _get_safe_default(t8)
    # Pre-mutate: t608 - differ = True - solver = False - old_items = ['t11', ' t607'] - old_hints = None
    try:
        t608 = t11(t607)
    except (TypeError, AttributeError, ValueError):
        t608 = _get_safe_default(t11)
    # Pre-mutate: t609 - differ = True - solver = False - old_items = ['difference', ' t608', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t609 = difference(t608, t13)
    except (TypeError, AttributeError, ValueError):
        t609 = _get_safe_default(difference)
    # Pre-mutate: t610 - differ = True - solver = False - old_items = ['size', ' t609'] - old_hints = ['Integer', 'Container']
    try:
        t610 = size(t609)
    except (TypeError, AttributeError, ValueError):
        t610 = _get_safe_default(size)
    # Pre-mutate: t611 - differ = True - solver = True - old_items = ['get_nth_f', ' t609', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t611 = get_nth_f(t609, F0)
    except (TypeError, AttributeError, ValueError):
        t611 = _get_safe_default(get_nth_f)
    # Pre-mutate: t612 - differ = True - solver = False - old_items = ['astuple', ' t610', ' t611'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t612 = astuple(t610, t611)
    except (TypeError, AttributeError, ValueError):
        t612 = _get_safe_default(astuple)
    s.append((612, 'ecdecbb3', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t612))
    # Pre-mutate: t613 - differ = True - solver = False - old_items = ['difference', ' t13', ' t608'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t613 = difference(t13, t608)
    except (TypeError, AttributeError, ValueError):
        t613 = _get_safe_default(difference)
    # Pre-mutate: t614 - differ = True - solver = False - old_items = ['size', ' t613'] - old_hints = ['Integer', 'Container']
    try:
        t614 = size(t613)
    except (TypeError, AttributeError, ValueError):
        t614 = _get_safe_default(size)
    # Pre-mutate: t615 - differ = True - solver = True - old_items = ['get_nth_f', ' t613', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t615 = get_nth_f(t613, F0)
    except (TypeError, AttributeError, ValueError):
        t615 = _get_safe_default(get_nth_f)
    # Pre-mutate: t616 - differ = True - solver = False - old_items = ['astuple', ' t614', ' t615'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t616 = astuple(t614, t615)
    except (TypeError, AttributeError, ValueError):
        t616 = _get_safe_default(astuple)
    s.append((616, 'ecdecbb3', 'differ_f4a4346405e974b919a4b5a52f28e40c', t616))
    # Pre-mutate: t617 - differ = True - solver = False - old_items = ['difference_tuple', ' t602', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t617 = difference_tuple(t602, t3)
    except (TypeError, AttributeError, ValueError):
        t617 = _get_safe_default(difference_tuple)
    # Pre-mutate: t618 - differ = True - solver = False - old_items = ['size', ' t617'] - old_hints = ['Integer', 'Container']
    try:
        t618 = size(t617)
    except (TypeError, AttributeError, ValueError):
        t618 = _get_safe_default(size)
    # Pre-mutate: t619 - differ = True - solver = True - old_items = ['get_nth_t', ' t617', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t619 = get_nth_t(t617, F0)
    except (TypeError, AttributeError, ValueError):
        t619 = _get_safe_default(get_nth_t)
    # Pre-mutate: t620 - differ = True - solver = False - old_items = ['astuple', ' t618', ' t619'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t620 = astuple(t618, t619)
    except (TypeError, AttributeError, ValueError):
        t620 = _get_safe_default(astuple)
    s.append((620, 'ecdecbb3', 'differ_fc9e44c913711da609a9e25126b4b296', t620))
    # Pre-mutate: t621 - differ = False - solver = True - old_items = ['sfilter_f', ' t513', ' t600'] - old_hints = ['FrozenSet', 'FrozenSet', 'Callable']
    try:
        t621 = sfilter_f(t513, t600)
    except (TypeError, AttributeError, ValueError):
        t621 = _get_safe_default(sfilter_f)
    # Pre-mutate: t622 - differ = False - solver = True - old_items = ['fork', ' combine', ' identity', ' t600'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t622 = fork(combine, identity, t600)
    except (TypeError, AttributeError, ValueError):
        t622 = _get_safe_default(fork)
    # Pre-mutate: t623 - differ = False - solver = True - old_items = ['difference_t', ' t513', ' t621'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t623 = difference_t(t513, t621)
    except (TypeError, AttributeError, ValueError):
        t623 = _get_safe_default(difference_t)
    # Pre-mutate: t624 - differ = False - solver = True - old_items = ['rbind', ' corner', ' R0'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t624 = rbind(corner, R0)
    except (TypeError, AttributeError, ValueError):
        t624 = _get_safe_default(rbind)
    # Pre-mutate: t625 - differ = False - solver = True - old_items = ['cover', ' t447', ' t623'] - old_hints = ['Grid', 'Grid', 'Patch']
    try:
        t625 = cover(t447, t623)
    except (TypeError, AttributeError, ValueError):
        t625 = _get_safe_default(cover)
    o.append((625, False, 'e21d9049', t625))
    # Pre-mutate: t626 - differ = True - solver = False - old_items = ['t1', ' t625'] - old_hints = None
    try:
        t626 = t1(t625)
    except (TypeError, AttributeError, ValueError):
        t626 = _get_safe_default(t1)
    # Pre-mutate: t627 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t626'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t627 = difference_tuple(t3, t626)
    except (TypeError, AttributeError, ValueError):
        t627 = _get_safe_default(difference_tuple)
    # Pre-mutate: t628 - differ = True - solver = False - old_items = ['size', ' t627'] - old_hints = ['Integer', 'Container']
    try:
        t628 = size(t627)
    except (TypeError, AttributeError, ValueError):
        t628 = _get_safe_default(size)
    # Pre-mutate: t629 - differ = True - solver = True - old_items = ['get_nth_t', ' t627', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t629 = get_nth_t(t627, F0)
    except (TypeError, AttributeError, ValueError):
        t629 = _get_safe_default(get_nth_t)
    # Pre-mutate: t630 - differ = True - solver = False - old_items = ['astuple', ' t628', ' t629'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t630 = astuple(t628, t629)
    except (TypeError, AttributeError, ValueError):
        t630 = _get_safe_default(astuple)
    s.append((630, 'e21d9049', 'differ_c59e6ae069a9205b311cf67a15473d64', t630))
    # Pre-mutate: t631 - differ = True - solver = False - old_items = ['t8', ' t625'] - old_hints = None
    try:
        t631 = t8(t625)
    except (TypeError, AttributeError, ValueError):
        t631 = _get_safe_default(t8)
    # Pre-mutate: t632 - differ = True - solver = False - old_items = ['t11', ' t631'] - old_hints = None
    try:
        t632 = t11(t631)
    except (TypeError, AttributeError, ValueError):
        t632 = _get_safe_default(t11)
    # Pre-mutate: t633 - differ = True - solver = False - old_items = ['difference', ' t632', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t633 = difference(t632, t13)
    except (TypeError, AttributeError, ValueError):
        t633 = _get_safe_default(difference)
    # Pre-mutate: t634 - differ = True - solver = False - old_items = ['size', ' t633'] - old_hints = ['Integer', 'Container']
    try:
        t634 = size(t633)
    except (TypeError, AttributeError, ValueError):
        t634 = _get_safe_default(size)
    # Pre-mutate: t635 - differ = True - solver = True - old_items = ['get_nth_f', ' t633', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t635 = get_nth_f(t633, F0)
    except (TypeError, AttributeError, ValueError):
        t635 = _get_safe_default(get_nth_f)
    # Pre-mutate: t636 - differ = True - solver = False - old_items = ['astuple', ' t634', ' t635'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t636 = astuple(t634, t635)
    except (TypeError, AttributeError, ValueError):
        t636 = _get_safe_default(astuple)
    s.append((636, 'e21d9049', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t636))
    # Pre-mutate: t637 - differ = True - solver = False - old_items = ['difference', ' t13', ' t632'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t637 = difference(t13, t632)
    except (TypeError, AttributeError, ValueError):
        t637 = _get_safe_default(difference)
    # Pre-mutate: t638 - differ = True - solver = False - old_items = ['size', ' t637'] - old_hints = ['Integer', 'Container']
    try:
        t638 = size(t637)
    except (TypeError, AttributeError, ValueError):
        t638 = _get_safe_default(size)
    # Pre-mutate: t639 - differ = True - solver = True - old_items = ['get_nth_f', ' t637', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t639 = get_nth_f(t637, F0)
    except (TypeError, AttributeError, ValueError):
        t639 = _get_safe_default(get_nth_f)
    # Pre-mutate: t640 - differ = True - solver = False - old_items = ['astuple', ' t638', ' t639'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t640 = astuple(t638, t639)
    except (TypeError, AttributeError, ValueError):
        t640 = _get_safe_default(astuple)
    s.append((640, 'e21d9049', 'differ_f4a4346405e974b919a4b5a52f28e40c', t640))
    # Pre-mutate: t641 - differ = True - solver = False - old_items = ['difference_tuple', ' t626', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t641 = difference_tuple(t626, t3)
    except (TypeError, AttributeError, ValueError):
        t641 = _get_safe_default(difference_tuple)
    # Pre-mutate: t642 - differ = True - solver = False - old_items = ['size', ' t641'] - old_hints = ['Integer', 'Container']
    try:
        t642 = size(t641)
    except (TypeError, AttributeError, ValueError):
        t642 = _get_safe_default(size)
    # Pre-mutate: t643 - differ = True - solver = True - old_items = ['get_nth_t', ' t641', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t643 = get_nth_t(t641, F0)
    except (TypeError, AttributeError, ValueError):
        t643 = _get_safe_default(get_nth_t)
    # Pre-mutate: t644 - differ = True - solver = False - old_items = ['astuple', ' t642', ' t643'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t644 = astuple(t642, t643)
    except (TypeError, AttributeError, ValueError):
        t644 = _get_safe_default(astuple)
    s.append((644, 'e21d9049', 'differ_fc9e44c913711da609a9e25126b4b296', t644))
    # Pre-mutate: t645 - differ = False - solver = True - old_items = ['rbind', ' corner', ' R3'] - old_hints = ['Callable', 'Callable', 'Any']
    try:
        t645 = rbind(corner, R3)
    except (TypeError, AttributeError, ValueError):
        t645 = _get_safe_default(rbind)
    # Pre-mutate: t646 - differ = False - solver = True - old_items = ['fork', ' connect', ' t624', ' t645'] - old_hints = ['Callable', 'Callable', 'Callable', 'Callable']
    try:
        t646 = fork(connect, t624, t645)
    except (TypeError, AttributeError, ValueError):
        t646 = _get_safe_default(fork)
    # Pre-mutate: t647 - differ = False - solver = True - old_items = ['t646', ' t592'] - old_hints = None
    try:
        t647 = t646(t592)
    except (TypeError, AttributeError, ValueError):
        t647 = _get_safe_default(t646)
    # Pre-mutate: t648 - differ = False - solver = True - old_items = ['t622', ' t647'] - old_hints = None
    try:
        t648 = t622(t647)
    except (TypeError, AttributeError, ValueError):
        t648 = _get_safe_default(t622)
    # Pre-mutate: t649 - differ = False - solver = True - old_items = ['intersection', ' t592', ' t648'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t649 = intersection(t592, t648)
    except (TypeError, AttributeError, ValueError):
        t649 = _get_safe_default(intersection)
    # Pre-mutate: t650 - differ = False - solver = True - old_items = ['fill', ' t598', ' EIGHT', ' t649'] - old_hints = ['Grid', 'Grid', 'C_', 'Patch']
    try:
        t650 = fill(t598, CYAN, t649)
    except (TypeError, AttributeError, ValueError):
        t650 = _get_safe_default(fill)
    o.append((650, True, '9aec4887', t650))
    # Pre-mutate: t651 - differ = True - solver = False - old_items = ['t1', ' t650'] - old_hints = None
    try:
        t651 = t1(t650)
    except (TypeError, AttributeError, ValueError):
        t651 = _get_safe_default(t1)
    # Pre-mutate: t652 - differ = True - solver = False - old_items = ['difference_tuple', ' t3', ' t651'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t652 = difference_tuple(t3, t651)
    except (TypeError, AttributeError, ValueError):
        t652 = _get_safe_default(difference_tuple)
    # Pre-mutate: t653 - differ = True - solver = False - old_items = ['size', ' t652'] - old_hints = ['Integer', 'Container']
    try:
        t653 = size(t652)
    except (TypeError, AttributeError, ValueError):
        t653 = _get_safe_default(size)
    # Pre-mutate: t654 - differ = True - solver = True - old_items = ['get_nth_t', ' t652', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t654 = get_nth_t(t652, F0)
    except (TypeError, AttributeError, ValueError):
        t654 = _get_safe_default(get_nth_t)
    # Pre-mutate: t655 - differ = True - solver = False - old_items = ['astuple', ' t653', ' t654'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t655 = astuple(t653, t654)
    except (TypeError, AttributeError, ValueError):
        t655 = _get_safe_default(astuple)
    s.append((655, '9aec4887', 'differ_c59e6ae069a9205b311cf67a15473d64', t655))
    # Pre-mutate: t656 - differ = True - solver = False - old_items = ['t8', ' t650'] - old_hints = None
    try:
        t656 = t8(t650)
    except (TypeError, AttributeError, ValueError):
        t656 = _get_safe_default(t8)
    # Pre-mutate: t657 - differ = True - solver = False - old_items = ['t11', ' t656'] - old_hints = None
    try:
        t657 = t11(t656)
    except (TypeError, AttributeError, ValueError):
        t657 = _get_safe_default(t11)
    # Pre-mutate: t658 - differ = True - solver = False - old_items = ['difference', ' t657', ' t13'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t658 = difference(t657, t13)
    except (TypeError, AttributeError, ValueError):
        t658 = _get_safe_default(difference)
    # Pre-mutate: t659 - differ = True - solver = False - old_items = ['size', ' t658'] - old_hints = ['Integer', 'Container']
    try:
        t659 = size(t658)
    except (TypeError, AttributeError, ValueError):
        t659 = _get_safe_default(size)
    # Pre-mutate: t660 - differ = True - solver = True - old_items = ['get_nth_f', ' t658', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t660 = get_nth_f(t658, F0)
    except (TypeError, AttributeError, ValueError):
        t660 = _get_safe_default(get_nth_f)
    # Pre-mutate: t661 - differ = True - solver = False - old_items = ['astuple', ' t659', ' t660'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t661 = astuple(t659, t660)
    except (TypeError, AttributeError, ValueError):
        t661 = _get_safe_default(astuple)
    s.append((661, '9aec4887', 'differ_d50e4f6db9fab7b53f413af7de3a35da', t661))
    # Pre-mutate: t662 - differ = True - solver = False - old_items = ['difference', ' t13', ' t657'] - old_hints = ['FrozenSet', 'FrozenSet', 'FrozenSet']
    try:
        t662 = difference(t13, t657)
    except (TypeError, AttributeError, ValueError):
        t662 = _get_safe_default(difference)
    # Pre-mutate: t663 - differ = True - solver = False - old_items = ['size', ' t662'] - old_hints = ['Integer', 'Container']
    try:
        t663 = size(t662)
    except (TypeError, AttributeError, ValueError):
        t663 = _get_safe_default(size)
    # Pre-mutate: t664 - differ = True - solver = True - old_items = ['get_nth_f', ' t662', ' F0'] - old_hints = ['Any', 'FrozenSet', 'FL']
    try:
        t664 = get_nth_f(t662, F0)
    except (TypeError, AttributeError, ValueError):
        t664 = _get_safe_default(get_nth_f)
    # Pre-mutate: t665 - differ = True - solver = False - old_items = ['astuple', ' t663', ' t664'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t665 = astuple(t663, t664)
    except (TypeError, AttributeError, ValueError):
        t665 = _get_safe_default(astuple)
    s.append((665, '9aec4887', 'differ_f4a4346405e974b919a4b5a52f28e40c', t665))
    # Pre-mutate: t666 - differ = True - solver = False - old_items = ['difference_tuple', ' t651', ' t3'] - old_hints = ['Tuple', 'Tuple', 'Tuple']
    try:
        t666 = difference_tuple(t651, t3)
    except (TypeError, AttributeError, ValueError):
        t666 = _get_safe_default(difference_tuple)
    # Pre-mutate: t667 - differ = True - solver = False - old_items = ['size', ' t666'] - old_hints = ['Integer', 'Container']
    try:
        t667 = size(t666)
    except (TypeError, AttributeError, ValueError):
        t667 = _get_safe_default(size)
    # Pre-mutate: t668 - differ = True - solver = True - old_items = ['get_nth_t', ' t666', ' F0'] - old_hints = ['Any', 'Tuple', 'FL']
    try:
        t668 = get_nth_t(t666, F0)
    except (TypeError, AttributeError, ValueError):
        t668 = _get_safe_default(get_nth_t)
    # Pre-mutate: t669 - differ = True - solver = False - old_items = ['astuple', ' t667', ' t668'] - old_hints = ['Tuple', 'Any', 'Any']
    try:
        t669 = astuple(t667, t668)
    except (TypeError, AttributeError, ValueError):
        t669 = _get_safe_default(astuple)
    s.append((669, '9aec4887', 'differ_fc9e44c913711da609a9e25126b4b296', t669))
    return o, s
